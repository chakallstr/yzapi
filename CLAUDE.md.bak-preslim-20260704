# CLAUDE.md

This file provides guidance to Claude Code (claude.ai/code) when working with code in this repository.

## Proje Özeti

**YZ API** — TL bakiye bazlı, OpenAI-uyumlu AI API gateway. Müşteri TL yükler, `yzk_live_*` API key alır, model kullanımı kadar bakiyeden düşülür. Akış: `Müşteri → https://yapayzekalab.org/v1 → model-bazlı provider profili (per-model routing) → upstream sağlayıcı`. `/v1/chat/completions` (OpenAI), `/v1/messages` (Anthropic-native, Claude Code), `/v1/responses`, `/v1/models` uçları + React panel.

- **Stack:** Express + TypeScript backend, React/JSX SPA frontend (Vite), PostgreSQL (Drizzle ORM), Vitest
- **Canlı (VPS):** `ssh yzapi-vps` → `/opt/turkapiprojesi`, systemd `turkapiprojesi` (local port **4568**), nginx reverse proxy → https://yapayzekalab.org
  - ⚠️ Deploy hedefi `/opt/turkapiprojesi`'dir — **`/opt/yapayzekalab` DEĞİL** (o eski/ayrı bir checkout; deploy/run scriptlerinde geçmez). `ssh vps` BAŞKA bir host'tur (seslab); bu repo için `ssh yzapi-vps` kullan.
  - ⚠️ env.ts default PORT 4567'dir; **canlı servis 4568'de** dinler (deploy scriptleri `SMOKE_BASE_URL=http://127.0.0.1:4568`).

---

## Komutlar

```bash
# Geliştirme
npm run dev          # tsx watch src/server/index.ts (backend + Vite dev middleware/HMR aynı entry'den)
npm run lint         # tsc --noEmit (sadece TypeScript tip kontrolü, emit yok)
npm test             # vitest run (DB gerektirmez; src/**/*.test.ts + __tests__/**/*.test.ts, mock-DB setup)
npm run test:watch   # vitest (interaktif watch)
npm run test:cov     # vitest run --coverage

# Tek test dosyası çalıştırma
npx vitest run src/server/services/billing-service.test.ts                              # tek UNIT testi (vitest.config.ts)
npx vitest run --config vitest.itest.config.ts src/server/__tests__/money-flow.itest.ts # tek ITEST (canlı Postgres şart)

# Integration (gerçek Postgres) — sıra ÖNEMLİ
npm run db:up        # docker compose up -d postgres
npm run db:migrate   # tsx src/server/db/migrate.ts
npm run itest        # vitest run --config vitest.itest.config.ts (src/**/*.itest.ts; fileParallelism:false; timeout 30s)

# E2E
npm run e2e:up       # bash scripts/e2e-up.sh (kurulum)
npm run e2e          # playwright test --project=chromium

# Build & sızıntı
npm run build        # rm -rf dist && vite build (frontend) + esbuild (backend → dist/server.js); postbuild .htaccess kopyalar
npm start            # node dist/server.js (build çıktısını çalıştırır)
npm run scan:public  # node scripts/scan-public-bundle.mjs — BUILT bundle'ı (dist/) tarar; build SONRASI çalışır, commit/deploy öncesi ŞART

# DB yönetimi
npm run db:migrate   # tsx src/server/db/migrate.ts (dotenv: NODE_ENV=production → .env.production, veya ENV_FILE_PATH)
npm run db:seed      # tsx src/server/db/seed.ts (idempotent; deploy ÇALIŞTIRMAZ — ayrı çalıştır)
npm run db:generate  # drizzle-kit generate
npm run db:studio    # drizzle-kit studio
npm run db:seed      # ...
npm run db:rotate-cipher    # tsx src/server/db/rotate-api-key-cipher.ts
npm run db:reset:users      # node scripts/reset-user-data.mjs --confirm (YIKICI)

# Ops / smoke
npm run smoke:vps       # node scripts/vps-smoke.mjs (SMOKE_BASE_URL ile sürülür; deploy gate içinde de koşar)
npm run preflight:live  # bash scripts/vps-live-preflight.sh
npm run ops:vps-status  # bash scripts/vps-ops-status.sh
npm run qa:uat          # node scripts/yapayzekalab-uat-smoke.mjs
```

### Deploy (canlı = ödeme sistemi — dikkat)
- **Gerçek deploy = `bash scripts/sync-deploy.sh`** — geliştirme makinesinden tek-komut, mekanizma **rsync** (git-pull DEĞİL):
  1. Lokal `/Users/ufuk/yzapi` → `yzapi-vps:/opt/turkapiprojesi` rsync (`.git/node_modules/.env*/dist` hariç; `--delete` YOK)
  2. SSH üzerinden server-side gate: `npm ci → lint → test → build → db:migrate (NODE_ENV=production .env.production) → systemctl restart turkapiprojesi → curl /health (http://127.0.0.1:4568)`
  3. `.deploy/` altına predeploy backup + manifest (lokal short commit dahil) yazar. Dry run: `bash scripts/sync-deploy.sh --dry-run`.
- **Yalnız COMMIT'li koddan deploy edilir.** Clean-tree guard: `git status --porcelain` boş değilse (untracked dahil) **abort eder** ("working tree kirli. Once commit le."). Önce commit'le.
- `/opt/turkapiprojesi` git reposu **DEĞİL** — deploy dosya senkronudur. Deploy `db:migrate` çalıştırır ama **`db:seed` çalıştırmaz** — seed/backfill ayrı koşar.
- **ÜÇ ayrı deploy path'i var, yalnız ilki güncel:**
  | Script | Komut | Hedef |
  |--------|-------|-------|
  | `sync-deploy.sh` | (doğrudan) | **GÜNCEL** — rsync → VPS `/opt/turkapiprojesi`, service `turkapiprojesi`, port 4568, clean-tree guard |
  | `vps-deploy.sh` | `npm run deploy:vps` | LEGACY — server-side; `/opt/turkapiprojesi`'nin git checkout olduğunu varsayar (`git rev-parse`/`git checkout` rollback), `.env.production` chmod 600 ister, rsync YAPMAZ. `sync-deploy.sh` ile çelişir (orada o dizin git değil). Kullanma. |
  | `deploy.sh` | `npm run deploy` / `deploy:fast` | FARKLI HEDEF — cPanel; zip'i Fileman API ile `/home/<CPANEL_USER>/yapayzekalab`'a yükler, Passenger restart, https://yapayzekalab.org/health. VPS ile karıştırma. |
- **Env-yükleme tuzağı (2026-06-11'de canlıda kanıtlandı):** `db/client.js` import eden scriptlerde `ENV_FILE_PATH=.env.production` **YETMEZ** — script içindeki `loadEnv(...)` ES-import **hoisting** yüzünden `lib/env.ts`'ten SONRA çalışır, env.ts ise ENV_FILE_PATH'i OKUMAZ (yalnız `NODE_ENV==='production' ? .env.production : .env`) → `.env` yüklenir, Zod `DATABASE_URL/JWT_SECRET Required` ile patlar. **Sunucuda doğru kullanım: `NODE_ENV=production npx tsx scripts/<seed>.ts`.** (`migrate.ts` istisna: kendi dotenv'i ENV_FILE_PATH'i okur.)

---

## Mimari

```
src/
├── App.tsx                   — route → initial tab; YapayZekaLabApp + TelegramTopupApp import'lar
├── index.html (REPO KÖKÜ) + src/main.tsx — Vite frontend entry; yapayzekalab/ İÇİNDE main.tsx/index.html YOK
├── master-models.ts          — MASTER_MODELS 42-KİLİT + canonicalizeModelId + ADDED_MODEL_DASH_TO_DOT köprüsü
├── pricing.ts                — fiyat formülü (carpan + USD→TL sell-kur)
├── server/
│   ├── app.ts                — createApp() factory (cron/Vite/listen YOK; supertest aynı route tree'yi sürer)
│   ├── index.ts              — entry: createApp → startKurRefresh + startAllJobs + Vite/static + app.listen
│   ├── routes/proxy.ts       — /v1/* → upstream; guard + reserve/settle billing + model çözümleme
│   ├── routes/{admin,user,payments,auth,models,settings,v1-catalog,telegram}.ts
│   ├── middleware/           — request-id, api-key-auth, admin-auth, user-auth, whatsapp-verified, error-handler
│   ├── services/             — closerouter-service (upstream adapter), provider-config-service, provider-adapter,
│   │                           billing-service, pricing-service, request-guard-service, api-settings-service,
│   │                           mali-izleme-*, payment stack, gozcu/* (sentinel) …
│   ├── jobs/                 — cron (index.ts → startAllJobs)
│   ├── db/{schema.ts,client.ts,migrations/}  — Drizzle tek-kaynak + sequential SQL (0000…0041, 42 dosya)
│   └── lib/{env.ts,errors.ts}  — Zod env (tek erişim noktası) + typed error hiyerarşisi
└── yapayzekalab/             — React/JSX SPA (tek-dosya: tab-*.jsx, api-docs.js, shared.jsx, auth-client.js)
```

### App factory & middleware sırası (`app.ts`)
`createApp()` Express app'i **cron/Vite/listener BAŞLATMADAN** kurar (supertest aynı route ağacını sürebilsin). Sıra:
`trust proxy=1` (tek nginx hop, XFF spoof'u engeller) → `express.json({limit:10mb, verify:rawBody})` (webhook imzası için) → `requestId` → `requestLang` (`req.lang` = X-Lang│Accept-Language│tr; lokalize API hataları) → `httpLogger` → **Gözcü tap** (`res.on('finish')`→`recordHttp(status)`; HER /api,/v1,/status response'unu in-memory metrics collector'a sayar, asla bloklamaz) → `/health` (auth/rate-limit yok; db SELECT 1 + kur yaşı + AI provider /models erişilebilirliği) → `/status` (public snapshot) → API/v1 mount'lar → `/api`,`/v1` için JSON 404 catch-all → `errorHandler` (en son). `index.ts`'teki `startServer()` ise `startKurRefresh()` + `startAllJobs()` + Vite dev middleware (dev) / static dist + SPA catch-all (prod) ekler, sonra `app.listen(env.PORT, env.HOST)`.

### Auth katmanlama (mount sırası — kritik)
- **Panel:** `/api/admin` (önce auth'suz `adminAuthRouter` login) → `/api/admin` (`adminAuth` + `requireWhatsappVerified` + `adminRouter`); `/api/auth` (login + **GitHub OAuth**: `GET /github` → redirect, `/github/callback`; `isGithubConfigured()` yoksa 503); `/api/user` (`userAuth` + `requireWhatsappVerified`); `/api` (models, settings, **packages+redeem**, support); `/api/payments`; `/api/telegram`. ⚠️ `packagesRouter` `/api`'ye mount edilir (userAuth arkasında DEĞİL) — purchase/redeem kendi auth'unu yapar.
- **`/v1` (3 katman):** önce auth'suz `v1CatalogRouter` (katalog) → whitelist 404 guard (yalnız `/balance`, `/chat/completions`, `/responses`, `/messages`, `/web-search`, `/images/*`, `/videos/*` geçer) → `/v1` (`apiKeyAuth` + `requireWhatsappVerified` + `proxyRouter`). Billing/proxy uçları **apiKeyAuth + whatsapp-verified** arkasında.
- **`apiKeyAuth`** (`api-key-auth.ts`): anahtarı **HEM `Authorization: Bearer yzk_live_` HEM `x-api-key: yzk_live_`** başlığından çıkarır (`extractProxyApiKey`; Authorization önce) — Anthropic-native istemciler (resmî **Claude Desktop** "Configure Third-Party Inference", Anthropic SDK) anahtarı `x-api-key` ile yollar, böylece yzapi gerçek Anthropic-uyumlu gateway. Yalnız `yzk_live_` ile başlayan değer geçer; eksik/geçersizde `recordAuthFailure(hashIp(ip))` (Gözcü auth_failure_spike) + 401; `validateApiKey` sonrası `req.apiKey`/`req.user` set eder. (CANLI 2026-06-18, ee7dd6d.)
- **`adminAuth`** (`admin-auth.ts`): JWT role==='user' + `durum==='aktif'` + rol çözümleme: `normalizeEmail(email)===ADMIN_EMAIL` → `"owner"`; `users.role==='partner'` → `"partner"`; aksi → 403. ⚠️ Owner **kod-sabiti email** ile tanınır: `ADMIN_EMAIL='cix.crazy666@gmail.com'` — DB rolüne bağlı değil. Express `Request` augmentation (admin?/user?/apiKey?) burada. Başarısızlıkta `recordAuthFailure`. **Partner RBAC** (`admin-permissions.ts`): `PARTNER_TABS = [dashboard, traffic, mali-izleme, gozcu, announce, payments, telegram, apikeys, logs, animations, users]` (11 sekme — ALL_TABS alt kümesi); `PARTNER_RULES` eşlenmemiş her yol → **fail-closed** owner-only. Owner her şeye erişir. Kullanıcıya partner rolü vermek: `PATCH /api/admin/users/:id/role` (owner-only); `users.role` kolonu migration `0025_user_role` (DEFAULT 'user').
- **`userAuth`** (`user-auth.ts`): Bearer ister, **`yzk_live_` key'leri açıkça reddeder** (API key panel route'larında çalışamaz); JWT role==='user' + aktif. `recordAuthFailure` ÇAĞIRMAZ.
- **`requireWhatsappVerified`** (`whatsapp-verified.ts`): OTP kapalıysa no-op; userId'yi `req.user?.id || req.apiKey?.userId`'den çözer (panel + API key kapsar); admin email ve `/me` GET/PATCH için bypass; yoksa 403 `{code:'whatsapp_verification_required'}`. Hem panel hem /v1 auth'undan sonra katmanlı.
- **`errorHandler`** (`error-handler.ts`): JSON-parse 400, `RateLimitError`→429 (`recordRateLimited()` + Retry-After), `AppError`→statusCode, beklenmeyen→500 (`recordUnhandled()`). Tüm response'larda requestId. (api-key-auth, admin-auth, error-handler → Gözcü collector besler.)

### Provider & Katalog sistemi (en kritik — çok dosya okumayı gerektirir)
- **PER-MODEL routing** (2026-06-03; ayrıntı: `~/.claude/.../memory/project_yzapi_per_model_routing.md`): her istek **modeline göre** farklı upstream'e gider. `resolveProviderForModel(masterModel.id)` (`provider-config-service.ts`) modeli `supportedModelIds`'inde içeren **enabled** profili bulur (**partition-by-supportedModelIds**: kümeler ÇAKIŞMAZ/disjoint) → o profilin `ProviderContext`'iyle (baseUrl/apiKey/modelMap) forward edilir; hiçbir profile pinlenmemiş model → `active_provider_id` fallback'i. **Canlı topoloji aşağıdaki tabloda (satır ~116)** — özet: Claude ailesi → `sub-claude` koltukları PRIMARY (wellflow ölü-yedek 402), GPT-5.5/5.4 codex paketleri → `sub-codex` koltukları (seat-primary), diğer GPT/o-serisi/Gemini → `closerouter`/Popusk. **metro + rika SİLİNDİ.** ⚠️ Eski "Claude → wellflow primary" ifadesi ARTIK YANLIŞ (wellflow kredisi bitti, koltuklara geçildi).
- **`provider_profiles`** (`id, baseUrl, apiKeyCipher, enabled, supportedModelIds, modelMap`) seed: `scripts/seed-provider-profiles.ts` (sunucuda ayrı koşar, deploy çalıştırmaz; `WELLFLOW_API_KEY`/`POPUSK_API_KEY`/`RIKA_API_KEY` env → DB cipher). `active_provider_id` artık yalnız FALLBACK (+ web-search + Gözcü `models_reachability` probe + connection-test). `AI_PROVIDER_*` env yalnız bootstrap.
- **Canlı provider topolojisi (2026-06-25 — SON):**
  | Profil | Handles | Upstream | Durum |
  |--------|---------|----------|-------|
  | `sub-claude` | **Claude PRIMARY** (opus-4-8/4-7/4-6, sonnet-4-6, haiku-4-5) | Mac mini `cliproxy:8317` autossh tünel → Ufuk+Kerim Codex koltukları | ✅ aktif |
  | `closerouter` | GPT-5/4.1/o-serisi, Gemini, Claude-dışı | `api.claude-popusk.shop` (popusk) | ✅ aktif |
  | `wellflow` | Claude yedek (boş `supportedModelIds`) | `api.wellflow.dev` | ❌ 402 (kredi bitti, uyku) |
  `rika` → **DISABLED**. `system_api_config.active_provider_id=closerouter` (fallback/probe).
- **`sub-claude` = koltuk tabanlı yönlendirme (cliproxy).** Müşteri Claude isteği → yzapi → `sub-claude` profili → autossh tüneli → Mac mini `cliproxy:8317` → Ufuk veya Kerim'in Codex koltukları. `cliproxy` bu makinede (`~/.cli-proxy-api/`, launchd `homebrew.mxcl.cliproxyapi`, port 8317); VPS tünel: `autossh -R 127.0.0.1:8317:127.0.0.1:8317 root@91.228.227.88`. ⚠️ **`/v1/messages` body'sine `user` alanı gönderme** — Anthropic API kabul etmez (`400 "Extra inputs"`); `proxy.ts`'te messages endpoint'inde `user` strip edilir (chat/responses'ta kalır).
- **`sub-codex` + `SEAT_PRIMARY_FOR_PACKAGE_GPT`:** CF codex paketleri (gpt-5.5/gpt-5.4) `SEAT_PRIMARY_FOR_PACKAGE_GPT` env flag'i set'liyken koltuk sağlayıcısına (`sub-codex`) yönlenir. `seatPrimaryPackageChain()` (`services/package-provider-override.ts`): `chain.primary.profileId !== "sub-codex"` ise `null` (CF'ye düşer). proxy.ts'te 3 call-site: `seatChain = env.SEAT_PRIMARY_FOR_PACKAGE_GPT && cfChain ? seatPrimaryPackageChain(chain, cfChain) : null; chain = seatChain ?? pkgChain`. Koltuk isteğinde `cf_remaining` düşmez → `shouldCapOverServe` (`cfServed > limit × capMult`) salt-CF-servis guard'ı bunu yakalar.
- **`CF_FIRST_FOR_PACKAGE_GPT` (CF-ÖNCE drain modu, 2026-06-25, CANLI+AKTİF):** seat-primary'nin TERSİ — açıkken codex paket isteği ÖNCE müşterinin CF'sine gider (`cfFirstPackageChain` = `{primary: CF, fallback: koltuk}`; sub-codex değilse null), CF bitince koltuğa düşer. Amaç: fazladan order edilmiş prepaid CF üniteleri süresi dolup yanmadan tüketilsin. `provider-failover.ts` `fallbackIsSeat` (fallback `sub-*`) → broad-eligible → CF 403/her-non-2xx → koltuk (müşteri kırılmaz, pre-commit guard korunur). `topUpCfIfNeeded` CF_FIRST açıkken codex'e YENİ sipariş VERMEZ ("içerideki para yenmez"; non-codex top-up çalışır). proxy.ts 3 call-site CF_FIRST > SEAT_PRIMARY öncelik. Default false=INERT. ⚠️ **drain bitince (~birkaç gün) `CF_FIRST_FOR_PACKAGE_GPT=false`+restart** (yoksa boş-CF müşterileri her istekte gereksiz CF-403→koltuk yapar). Bkz `project_yzapi_cf_drain_and_codex_daily_gate` memory notu.
- **`CODEX_SPARK_ALTERNATION_ENABLED` (Codex 1-1 spark alternasyonu, 2026-07-02 CANLI+STABİL):** açıkken bir codex GPT PAKET isteğinde gpt-5.5/gpt-5.4 müşteri-başı `requests_today` **paritesine** göre 1-1 bölünür — ÇİFT (idx%2==0) → zincir DEĞİŞMEZ (gerçek model, "premium" kova); TEK → zincirin KOLTUK (`sub-codex`) bacağı upstream'e `gpt-5.3-codex-spark` (bengalfox — daha hafif model) gönderir + yanıt müşterinin istediği isme **relabel** edilir (tel-üstü `codex-spark` adı sızmaz; `applyCodexSparkAlternation` `relabelResponseTo` set eder, kimlik-koruma system-prompt'u eklenir). Yalnız `sub-codex` primary'li zincire dokunur; fallback = GERÇEK koltuk (spark patlarsa müşteri gerçek modeli HATASIZ alır), izole breaker `sub-codex:spark`. Kod: `applyCodexSparkAlternation(chain, model, requestsToday, requestBody)` (`package-provider-override.ts`, SAF, 4-arg), proxy.ts **3 text call-site** `if (env.CODEX_SPARK_ALTERNATION_ENABLED && billedViaPackage)`. ⚠️ **EKONOMİ (kanıtlı, kolay yanlış anlaşılır):** spark VE gerçek gpt-5.5 aynı Codex koltuğundan servis edilir AMA **AYRI KOTA KOVALARI** (ChatGPT hesabında ayrı limit bölümleri: "Genel" vs "GPT-5.3-Codex-Spark") → spark = "aynı havuzdan hafif çeken model" DEĞİL, **bağımsız ikinci kota deposu** (haftalık, kullan-ya-da-kaybet); kapatmak o kapasiteyi çöpe atar. Ölçülmemiş değişken = kalite deltası (5.3-spark vs 5.5). Ufuk kararı (07-02): **1-1'de kal, kalite-benchmark yok.** Bkz `project_yzapi_codex_1to1_spark_alternation` + `project_yzapi_stream_zero_output_diag_2026_07_02` + `project_yzapi_private_codespark_model` memory notları.
- **Spark UYGUNLUK KAPISI = `sparkEligibleRequestBody` (4-tarama, 2026-07-02, `package-provider-override.ts`):** spark modeli gpt-5.5'in TÜM yeteneklerini taşımaz → alternasyon YALNIZ uygun isteği spark'a sokar (uygunsuz → zincir aynen, gerçek model tam yetenekle servis eder, o istekte tasarruf yok ama müşteri HATASIZ). 4 kısıt, hepsi canlıda ayrı olaylarla kanıtlandı + kapı ile kapatıldı: **(1) reasoning.context** — spark yalnız `auto`/`current_turn` kabul eder (`all_turns`→400); `normalizeSparkReasoningContext` tel-üstü model spark ise desteklenmeyen context'i mutasyonsuz `auto`'ya çevirir (bu TAMİR eder, engellemez — `applyProfileModelMap` içinden `closerouter-service.ts`'te 5 forward yolunu tek noktadan kapsar). **(2) built-in araç allowlist** — `SPARK_SUPPORTED_TOOL_TYPES = {function, custom, web_search, tool_search, namespace}`; `image_generation` vb. → uygunsuz. **(3) multimodal içerik** — `SPARK_SUPPORTED_CONTENT_PART_TYPES = {input_text, output_text, text, refusal, summary_text}`; görsel/dosya/ses parçası (input_image/image_url/input_audio/input_file) → uygunsuz (spark VISION DEĞİL). **(4) gövde boyutu** — `SPARK_MAX_BODY_BYTES = 192*1024` (spark context ≈128k token; kanıt: 256KB gövde OK, 320KB+ stream-içi `context_too_large`); üstü uygunsuz. **⚠️ SPARK DEGRADE-BYPASS güvenlik ağı (proxy.ts `/v1/responses` STREAM handler):** spark bacağında (`ctx.relabelResponseTo` set) native 400 artık çeviri yoluna DÜŞMEZ (çeviri aynı spark map'iyle yine 400 + SSE başlıkları post-commit → chain-failover ÖLÜR = `stream_zero_output`) → **RETHROW** → `forwardWithFailover` sub-codex geniş taksonomisiyle isteği GERÇEK koltuğa düşürür = gelecekteki HER bilinmeyen spark kısıtı için ağ. ⚠️ **Bu kod (spark alternasyon + 4 kapı + degrade-bypass) canlıda targeted-rsync ile var ama lokal `~/yzapi` kaynağında YOK** (geri commit'lenmedi); canlı-faithful worktree `~/yzapi-fairness-work` (commit `afa1dcb`). Her fix TDD RED→GREEN + 3/3 adversarial QA + izole rsync.
- **Codex paket gate = DAILY model (2026-06-25, CANLI):** `entitlement-service.ts` 2 gate fn (checkPackageCoverage+tryReservePackageSlot, lockstep) `cf_api_slug='codex-api'` paketleri için: günlük cap zorlanır (`requests_today < daily_limit_snapshot`, gün dönünce reset — non-devreden çok-günlük codex'in Gün-2 kilidi + over-serve 936/500 fix) + CF aynası bloklamaz (Clause 2 `cf_api_slug='codex-api' OR ...` → koltuk servis ediyor). `topUpCfIfNeeded` codex non-devreden tavanı `daily_limit_snapshot × sure_gun` (multi-day). Non-codex CF (glm/composer) BYTE-IDENTİK.
- ⚠️ **`CF_PROACTIVE_TOPUP_ENABLED=false` (2026-06-25):** `cf-mirror-sync` job proaktif CF top-up'ı KAPATILDI. Sebep (over-order olayı): codex topUp tavanını açan fix ile onu sınırlayan proactive-skip guard'ı AYRI deploy edilince, arada ~70dk pencerede job 3 codex müşterisine ~1350 ünite (~675 TL) peşin CF order verdi → reseller cüzdanı 15.69 TL'ye düştü. **DERS: tavanı-AÇAN fix + onu SINIRLAYAN guard ASLA ayrı deploy etme — arada proaktif job/oto-mekanizma boşluğu sömürür.**
- **Canlı provider topolojisi (2026-06-09 — TARİHSEL):** `rika` → DISABLED; `wellflow` → yalnız Claude; `closerouter/Popusk` → GPT/Gemini. Metro silindi. **`codefast`** (enabled, `reseller-api.codefast.app`, `modelMap={}` = canonical verbatim) → CF reseller'ın 18 modeli (GLM/Grok/Kimi/DeepSeek/NVIDIA/MiniMax/Step/composer; `provider_label='YapayZekaLab'`, slug `yapayzekalab`). ⚠️ Bu profil **paket-override'dan AYRI** bir genel per-model routing hedefidir → bu 18 model **paketsiz, token-başına PAYG** da çağrılabilir; fiyatları `added_models.input_usd/output_usd`'de (2026-06-20 set edildi: bütçe $0.30 / orta $0.50 / premium $0.80, in=out; öncesi ₺0 = bedava-kullanım deliğiydi — bkz `project_yzapi_models_provider_crash` memory notu). CF **istek-başına** faturalar (token değil) → küçük istek CF maliyetinin altında kalabilir. ⚠️ **CF reseller'ın ~5MB istek-gövde limiti var** (2026-06-27 probe, `/proxy/<slug>/v1`: ~1MB→200, ≥5MB→`400 {"error":"Hata Oluştu"}`) — bu HER CF çağrısını etkiler (paket-override + **seat→CF fallback** + PAYG): bir koltuk-paketi müşterisi dev `remote_compaction_v2` gövdesi (15-17MB) yollarsa seat timeout/abort olur VE CF fallback 5MB'ı aşıp 400 döner → iki bacak da ölür (normal ≤1MB istek sorunsuz). Bkz `project_yzapi_codex_seat_429_spof_tpm_cap` memory notu. `system_api_config.active_provider_id=closerouter` (fallback/probe). `default_context_limit_tokens=1000000` (2026-06-14 128000→1M yükseltildi: 128k global guard opus-4.8'i 1M penceresinden 128k'da kesiyordu; efektif limit `min(global, masterModel.contextTokens)` olduğu için 1M'de her model kendi gerçek penceresiyle sınırlanır — opus/sonnet/gpt-5→1M, haiku/o3/o4→200k; canlı `system_api_config` UPDATE, deploy/restart yok), `default_max_tokens_per_request=128000` (ÇIKTI rezervi, ayrı kavram), `default_stream_timeout_ms=300000`.
- **`setActiveProvider(id)`** yalnız `enabled` kontrol eder — **reachability/health gate YOK**. Per-model routing'de aktifi değiştirmek pinli modelleri YENİDEN yönlendirmez (onlar `supportedModelIds`'e bağlı); bir upstream'i boşaltmak için profili disable et / `supportedModelIds`'i taşı. `failover_provider` heal (default off) yalnız `active_provider_id`'yi çevirir → pinli modeli kurtarmaz (bilinen kısıt).
- **Katalog = UNION:** `resolveSupportedModelIds()` = tüm enabled profillerin `supportedModelIds` BİRLEŞİMİ (sıfır profil → `null` = "hepsini göster" geriye-uyum). `/api/models` + `/v1/models` + request-guard allowlist bunu kullanır; hiçbir profilde olmayan model → 404. **`modelMap`** = canonical-id → upstream-wire-id (per-profil). `strictCanonicalModelIds` (default **true**) → proxy upstream'e `masterModel.id` (canonical) gönderir; modelMap wire-id'ye çevirir. **Fiyat:** müşteri USD/1M `model_overrides` ile per-model override edilir (`notlar='faz3 fiyat'`; carpan UYGULANMAZ — customerInputUsd doğrudan).
- **MASTER_MODELS 42-kilit** (`src/master-models.ts`): yeni model **EKLENMEZ**; `added_models` DB katmanında. `canonicalizeModelId` + `ADDED_MODEL_DASH_TO_DOT` client dash formu (`claude-opus-4-8`) → canonical dot (`claude-opus-4.8`) çevirir. ⚠️ **İstemcinin sabit-kodladığı, katalogda OLMAYAN yan-çağrı model id'leri** (örn. Codex CLI'ın auto-review sidecar'ı `codex-auto-review`; aksi halde her riskli-komut incelemesi 404 "Model bulunamadı") mevcut bir entry'ye `aliases:[...]` ile bağlanır → `canonicalizeModelId` onu canonical id'ye çözer (gpt-5.5), istek o model gibi faturalanır/kapılanır (coverage için aynı modele bağla, ucuz bir modele DEĞİL). **Alias 42-kilidi BOZMAZ** (entry sayısı/id değişmez; `added-model-catalog-lock.test.ts` snapshot'ı yalnız `{id,customerInputUsd,customerOutputUsd}` tutar) ve **`/v1/models`'te GÖRÜNMEZ** (katalog id'lerden üretilir, aliasten değil; `models.ts`/`v1-catalog.ts`). Bkz `project_yzapi_codex_auto_review_alias` memory notu.
- **Provider adapter:** `provider-adapter.ts` ProviderAdapter + `closerouter-service.ts` forward fn'leri artık son parametre **`ctx: ProviderContext`** alır (`resolveProviderForModel`'den; self-resolve YOK). `getActiveProviderAdapter()` yalnız implementasyonu seçer — **upstream'i `ctx` belirler**. `forwardChat`(/chat/completions), `forwardTextEndpoint`(/messages), `forwardChatStream`(SSE), `forwardChatStreamAsResponses`(/responses streaming köprüsü → chat SSE'yi Responses event'lerine çevirir); `applyProfileModelMap(body, ctx.modelMap)` + `baseHeaders(ctx.apiKey)` (ikisi de sync). Yeni provider eklerken model adı formatını **gerçek endpoint'te** doğrula (itest nock yakalamaz; canlı `GET <base>/v1/models` probe et); key'ler env'den seed'e → DB cipher, asla hardcode.

### Provider Failover & Circuit Breaker (`fix/upstream-connect-retry`)
Request-seviyesi cross-provider failover — Gözcü'nün `failover_provider` heal'inden (yukarıda; yalnız `active_provider_id`'yi çeviren) **AYRI** mekanizma.
- **`forwardWithFailover(chain, opts, runForward)`** (`services/provider-failover.ts`): primary profili `FAILOVER_PRIMARY_BUDGET_MS` (**7sn**) bütçeyle çalıştırır; yalnız **pre-commit** (response header'ı GİTMEDEN) + **eligible** hatada fallback'e **bir kez** geçer. `{result, servedBy, failedOver}` döner. Eligible = 502/503/504 + bağlantı hataları (ECONNREFUSED/ENOTFOUND/timeout) + bütçe AbortError; non-eligible (4xx, 500/501) veya post-commit → breaker'ı reachable işaretler, failover YOK (stream zaten akmaya başladıysa düşmez).
- **Circuit breaker** (`services/provider-circuit-breaker.ts`): **in-memory** (DB yok), per-profil/per-process. **3 hata → open** (primary atlanır, doğrudan fallback) → **60sn → half-open** (tek probe; `halfOpenInFlight` atomik) → `recordReachable()` → closed.
- **`closerouter-connect-retry.ts`**: upstream'e bağlanırken (socket connect) kısa retry — connect aşaması hatasını gerçek upstream hatasından ayırır.
- **Şema:** migration `0024` → `provider_profiles.fallback_provider_id` (nullable, **FK YOK** soft-ref; nil = failover kapalı). Admin/seed set ederken validation.
- **Wiring:** `proxy.ts`'te **5 call-site** (chat/completions, messages, responses, 2 image handler). **Env flag YOK** — `fallback_provider_id` set'liyse açık; değilse no-op. Pinli per-model routing'i bozmaz (chain `resolveProviderForModel`'den gelir).

### Billing Akışı (proxy.ts → billing-service.ts)
Her text ucu `enforceRequestGuards()` koşar (paralel: model çözümleme, runtime config, apiKey policy, rate limit, model allowlist, bakiye>0 ön-guard) → `buildRequestGuard()` effective context/max-output limitlerini `min()` ile hesaplar.
- **Sampling-param strip (2026-06-15, CANLI):** Opus 4.7'den itibaren (4.7, 4.8) + Fable/Mythos `temperature`/`top_p`/`top_k`'yi REDDEDER (upstream 400 "temperature is deprecated for this model"). Cline/Roo/Codex bunları VARSAYILAN gönderir → opus-4.8'i kırıyordu. `master-models.ts modelRejectsSamplingParams(id)` (kanonik id'yi tireye normalize edip `{opus-4-8, opus-4-7, fable-5, mythos-5}` setiyle eşler — dash+dot) → `buildRequestGuard({rejectsSamplingParams})` true ise bu 3 param `guardedBody`'den silinir + aralık doğrulaması atlanır. proxy.ts 3 call-site'ta geçilir; stream+non-stream ikisi de `guard.guardedBody`'den kurulduğu için kapsanır. opus-4.6/sonnet-4.6/haiku temperature'ı HÂLÂ kabul eder — DOKUNULMAZ. Detay: `project_yzapi_sampling_param_strip` memory notu.
- **Extended thinking / `anthropic-beta` header iletimi (2026-06-22, CANLI):** Anthropic `/messages` ucu extended thinking için (1) `thinking: {type:"enabled", budget_tokens: N}` body'de gerektirir — `budget_tokens` eksikse upstream Pydantic 422 döner; (2) upstream `anthropic-beta: interleaved-thinking-2025-05-14` header'ı bekler. İki fix: `buildRequestGuard()` (`request-guard-service.ts`) `thinking.type==="enabled" && budget_tokens==null` → `max(1024, min(8000, reservedCompletionTokens-1))` varsayılan enjekte eder; `proxy.ts` `handleTextJsonEndpoint` client `req.headers["anthropic-beta"]`'yı yakalar → `messagesUpstreamHeaders` olarak `forwardMessages(providerBody, ctx, attempt, messagesUpstreamHeaders)` ile iletir. `forwardTextEndpoint` (`closerouter-service.ts`) artık opsiyonel son parametre **`upstreamHeaders?: Record<string, string>`** alır; `{...baseHeaders(ctx.apiKey), ...upstreamHeaders}` ile birleştirir. Bu pattern yalnız `/messages`'ta geçerli — `/responses`/`/chat` ayrı fn'ler (`forwardResponses`/`forwardChat`), upstream-header-pass YOK. Aynı deploy'da `readProviderJson` HTML yanıtında parse-crash (→ 500) da düzeltildi: try/catch + anlamlı hata mesajı.
- **Token Saver (2026-06-19, CANLI, flag-kontrollü):** `services/token-saver.ts` — tool/araç ÇIKTILARINI (OpenAI `role:"tool"`, Anthropic `tool_result` blokları) upstream'e gitmeden sıkıştırır (ANSI strip + boş-satır/3+tekrar katla + >32KB baş/son kırpma); kullanıcı mesajına/koduna ASLA dokunmaz; saf, idempotent, ASLA throw etmez (hata→orijinal). `closerouter-service.ts` 4 forward fn'inde (forwardChat/forwardTextEndpoint/forwardChatStream/forwardChatStreamAsResponses) `maybeCompressToolOutputs(providerBody, runtimeConfig)` → **tüm sağlayıcılara** (popusk/wellflow/CF) uygulanır. Kill-switch `system_api_config.token_saver_enabled` (migration 0039, default false→inert; `getRuntimeApiConfig` cache'siz → DB UPDATE anında etki). Billing otomatik (upstream az token raporlar→müşteri az öder; `resolveBilledPromptTokens` provider'a güvenir). ⚠️ ReDoS dersi: ANSI OSC regex'i sınırlı (`{0,512}`) + no-ESC early-exit + istek-başı iş bütçesi (sınırsız `[^BEL]*` katastrofik backtracking'di). Detay: `project_yzapi_cf_lazy_mirror` memory notu.
1. `reserveUsageBudget()` — atomic `UPDATE users SET bakiye_tl -= cost WHERE bakiye_tl >= cost` + `kullanim_rezervasyon` transaction (key `usage_reserve_<reqId>`); yoksa `InsufficientBalanceError` → **402**
2. upstream forward → `normalizeProviderUsage()` (OpenAI: `prompt_tokens` taban, **cache çift sayılmaz**; Anthropic: `input + cache_read + cache_create`); ham usage `providerRaw` olarak saklanır
3. `resolveBilledPromptTokens(providerNormalized, serverContext)` — provider **>50** → ona güven (char/4 ile şişirme YOK, büyük-JSON over-billing'i önler); **≤50** (bozuk/sıfıra-yakın rapor) → `max(provider, serverContext)` floor (under-charge = bizim zarar). Sadece SAYIYI seçer, formülü değiştirmez.
4. `settleReservedUsage()` — tek `dbSql.begin` içinde: rezervasyonu iade (`iade`, `usage_release_<reqId>`) → `actualCost>0` ise gerçek maliyeti tahsil (`kullanim`, `usage_final_<reqId>`, **balance guard YOK** — servis verildi, overage'da hafif eksiye düşebilir) → **her zaman** bir `usage_records` satırı yazar (idempotent: `request_id` UNIQUE).
- **K1:** upstream hatasında **0 tahsil** + tam iade (usage_records status='error', cost 0). Hatada bile usage satırı yazılır; upstream error body `forwardUpstreamError` ile iletilir.
- Billing header'lar: `X-YZ-Cost-TL` / `X-YZ-Remaining-TL` / `X-YZ-Remaining-USD` / `X-YZ-Request-Id`.
- **`chargeUsage()`** = rezervasyonsuz fallback (key `usage_<reqId>`); error/0-cost'ta bakiyeye dokunmadan sadece usage satırı yazar.

### /v1 uç envanteri
- Gerçek: `POST /chat/completions` (stream + `web_search` auto-augment destekli), `GET /balance` (TL+USD+kur), `POST /responses` (OpenAI Responses API → **`handleResponsesEndpoint`**; Codex CLI), `POST /messages` (Anthropic-native → `handleTextJsonEndpoint`), `POST /images/generations`+`/images/edits` (görsel — CANLI, sabit per-image ücret; aşağıya bak), `POST /web-search` (ayrı, sabit $0.001/arama ücreti).
- **Responses API kabuğu (Codex CLI):** Codex ≥0.99 yalnız OpenAI Responses API'sini (`/v1/responses`) konuşur (`wire_api="chat"` Şubat 2026'da KALDIRILDI) ama upstream'ler `/responses` sunmaz (404). `handleResponsesEndpoint` (`proxy.ts`) gelen Responses isteğini chat/completions'a çevirir (`services/responses-translation.ts`: `responsesRequestToChat` / `chatCompletionToResponses` / `ResponsesStreamTranslator`), modeli **endpoint `"chat"`** ile çözer (böylece "model does not support responses" 400'ü oluşmaz; `allowResponsesEndpoint` toggle'ı `buildRequestGuard` ile korunur), `forwardChatStreamAsResponses` (`closerouter-service.ts`; `forwardChatStream`'in İKİZİ — chat hot-path'e DOKUNULMAZ) ile chat SSE'yi Responses event dizisine çevirir. Billing chat handler ile **birebir parite** (reserve/settle/`resolveBilledPromptTokens`). Codex model slug alias: `gpt-5.x-codex`→`gpt-5.x`. Doküman Codex kartı varsayılanı **`gpt-5.5`**.
- **Web search billing izolasyonu:** `web_search` upstream'e gönderilmeden önce strip edilir; settle SONRASI ayrı sabit $0.001/arama ücreti `chargeWebSearch` ile (key `ws_<reqId>`, reserve/settle'a DOKUNMAZ). Standalone `/web-search` yalnız sonuç varken charge eder.
- **Görsel uçları CANLI (Faz4a); video hâlâ 501.** `POST /images/generations` + `/images/edits` artık gerçek forward: `requireProxy` → `withImageSlot()` kuyruğu (`image-queue.ts`) → `adapter.forwardImage(endpoint, body, ctx)` (`closerouter-service.ts`; `{baseUrl}/images/{endpoint}`'e POST, `imageCount` döner) → **sabit per-image ücret `chargeImage()`** (`image-billing-service.ts`; token reserve/settle yoluna GİRMEZ — web-search gibi sabit-ücret, key benzeri idempotency). Katalogda görsel model artık **temsil edilir**: `added_models`'te `type` + `image_price_usd` kolonları (migration `0021`); `addedModelToMasterModel` `row.type==="Görsel"` → `type:"Görsel"` + `outputModalities:["image"]` + `IMAGE_ENDPOINTS` + `providerImageInputUsd: row.imagePriceUsd` döndürür; `v1-catalog.ts`/`models.ts` görsel override patch'i; `pricing.ts` `computeImagePrice`. **(Bu üç eski eksiğin — 501 route, katalog temsili, `chargeImage` — hepsi artık kapalı.)** Yalnız `POST /videos/submit` + `GET /videos/tasks/:id` sabit **501** döner (Phase D — `submitVideo`/`getVideoTask` adapter hazır ama route/billing/katalog yok). Tüm gerçek uçlar `requireProxy` (upstream key yoksa 503) + `maintenanceModeForApi` (503) ile sarılı.
- **Görsel üretimi MÜŞTERİ-YÖNÜ CANLI (GPT Image 2, CF-served, 2026-06-27).** Sekme "Görsel" (`tab-studio.jsx`, AI Chat'in yerinde) + `/v1/images/generations`. Akış = CF chat paket akışının görsel ikizi: `handleImageEndpoint` (proxy.ts) `tryReservePackageSlot` → `entitlementOverrideChain(slot, env.CODEFAST_RESELLER_BASE_URL).primary` → `forwardImage` → CF proxy. Paket yolu kotadan düşer (cost=0), bakiyeden DEĞİL; PAYG görsel upstream'i YOK → paketsiz istek temiz **402** ("görsel paketi gerekli" + panel CTA), codefast `/images` 404'üne GİTMEZ. Seed: `scripts/seed-image-packages.ts` (added_models `gpt-image-2` + codefast profili `supported_model_ids`'e ekler + `img-50`/`img-100` paketleri). Detay + tüm tuzaklar: `~/.claude/.../memory/project_yzapi_cf_image_gen_works.md`. **HARD-WON GOTCHA'lar (canlı kanıtlı):**
  - **CF görsel slug `gpt-image-2-api`** (catalog slug `gpt-image-2-studio__gpt-image-2-api`'nin __'den SONRAKİ yarısı); `-studio` → 404 "API_NOT_FOUND". Eski paketler `-studio` ile snapshot'lı → `package-provider-override.ts normalizeCfSlug` routing anında `-api`'ye çevirir (DB yazımı yok).
  - **CF `size` VE `aspect_ratio`'yu DOĞRULAR ama ÜRETİMDE YOK SAYAR** — çıktı hep ~1122×1402/1254×1254. `size`+`aspect_ratio` uyuşmazsa **400** ("Use 1536x864" der ama o boyutu da üretmez). Kesin boyut TEK yolu: `image-resize-service.ts` ffmpeg ile çıktıyı istenen W×H'ye **cover+crop resize** (VPS'te `/usr/local/bin/ffmpeg`, npm dep YOK; never-throw → hata=orijinal). Studio `aspect_ratio` GÖNDERMEZ (400 riski) → `size`(=oran→sabit px) + `quality` gönderir, backend resize eder.
  - **Görsel CF mirror**: yanıt `x-codefast-remaining` header'ı (chat'teki gibi, `cfRemainingHeader`) → `forwardImage` döndürür → handler `updateCfRemaining(userId, rem, "success")` ile entitlement.cf_remaining'e yazar → panel `kalan = min(daily_limit_snapshot, cf_remaining)` senkron (donuk-sayaç fix). ⚠️ Eğer bir entitlement'ın CF havuzu > `daily_limit_snapshot` ise panel limit'te kapanır (hediye-paket veri uyumsuzluğu; limit'i havuza hizala).
  - **504 önleme**: görsel gen YAVAŞ (40-120s) + ara sıra geçici "upstream returned an error". `handleImageEndpoint` ZAMAN-BÜTÇELİ retry yapar — `forwardImage(…, timeoutMs)` per-attempt **230s** (param + `maxAttempts=1`), yalnız HIZLI(<20s) geçici hatada 1 retry → toplam < nginx 300s → **504 YOK**. Naif 3× retry yavaş upstream'de katlanıp 504 verirdi. Başarısız deneme kota/CF TÜKETMEZ (resize/charge yalnız başarıda) → retry para-güvenli.
  - **Paket modeli TEK-GÜNLÜK** (`sure_gun=1`, gunluk≤150): CF non-codex `gunluk_istek_limiti` = ÖMÜRLÜK ünite tavanı (günlük reset YOK); çok-günlük görsel paketi CF'ye 30× fazla ödetir (maliyet = 0.45₺ × limit × gün). Çok-günlük = faz-2.

### Paketler, Redeem & Account Delivery (entitlement — Faz1/Faz2)
Token-bazlı billing'e **paralel** ikinci tüketim yolu — `system_config.packages_enabled` flag'iyle gate'li.
- **Tablolar** (`0020`/`0021`/`0023`/`0026`–`0032`): `packages` (günlük istek limiti, süre_gün, `allowed_models` whitelist jsonb, fiyat TL/USD, enabled, `is_configurable`+min/max 0026, `max_context_tokens` 0027, `tpm_limit` 0029, **`satista`** 0030, **`provider_base_url`+`provider_api_key_cipher`** 0032), `user_package_entitlements` (kullanıcı başına aktif hak + günlük sayaç/reset), `redeem_codes` + `redeem_code_uses` (promo/hediye kod; tip=bakiye│paket, `max_uses`/`per_user_once`/`expires_at`), `account_delivery_orders` (hesap-teslim siparişi; durum `bekliyor→teslim_edildi`).
- **Redeem-only & onay & kişi-başı-limit (2026-06-18, CANLI):** `kategori='Deneme'` paketler gridden GİZLİ + doğrudan satın alınamaz, yalnız redeem-kod/key (`listPublicPackages`/`getPublicPackage` `<> 'Deneme'` + purchase'ta kategori guard). `packages.per_user_once=true` → kişi başı 1 kez (free-tier abuse koruması; `user_package_entitlements`'ta kayıt varsa 400; ömür-boyu, guard tx-öncesi). Admin "Kodlar" sekmesi: key'ler SİLİNMEZ; `redeem_codes.copied_at` (admin "Kopyala" → panoya al + dağıtıldı işaretle) veya `used_count>0` → satır kararır. `scripts/gen-test-keys.ts` (sunucuda `NODE_ENV=production npx tsx`) toplu key üretir. **İade-politikası zorunlu onay kapısı** (tab-account "Bakiye Yükle"): `refundOk` onaylanmadan ödeme alanı **bulanık+kilitli** (blur + pointerEvents:none + öde butonu `disabled` + `onTopUp` erken-return — klavye bypass kapalı); her ziyarette tekrar onay; metin AML/iade-yok + hatalı/mükerrer-tahsilat istisnası (i18n `account.refund.*`, TR/EN).
- **`satista` (yakında-satışta modu, 0030):** `enabled=true & satista=false` → paket katalogda GÖRÜNÜR ("YAKINDA" rozeti + devre dışı "En kısa sürede satışta!" butonu) ama **satın alma VE redeem 400 döner** — gate her iki yolda da para hareketinden ÖNCE (`package-purchase-service.ts` + `redeem-code-service.ts`). Satışa açma: admin Paketler → "Satışa Aç" (PATCH `{satista:true}`). `enabled=false` ise paket tamamen gizli (eski davranış).
- **Paket-bazlı upstream override (0032):** `provider_base_url`+`provider_api_key_cipher` İKİSİ DE doluysa paket kapsamındaki istek o endpoint'e gider — `packageOverrideChain()` (`package-provider-override.ts`) tek-sağlayıcılı zincir döner (`profileId="pkg:<id>"`, **fallback/breaker YOK**, `modelMap:{}` = canonical id verbatim); boş/çözülemez key → `null` = normal per-model routing (istek asla kırılmaz). proxy.ts'te 3 text call-site'ta `billedViaPackage` sonrası zincir değişimi. Doldurma admin "API" butonu → POST `/api/admin/packages/:id/provider` (owner-only, http(s) URL validasyonu; key cipher'ı GET listesinde STRIPlanır → `providerKeySet` boolean) — **deploy gerektirmez** (slot her istekte DB'den taze okur).
- **Context token limiti:** `packages.max_context_tokens` (nullable; set'li pakette `tryReservePackageSlot` DB JOIN ile döner → `PackageCoverage.maxContextTokens`). `proxy.ts` 3 call-site'ta: `guard.contextTokens > pkgSlot.maxContextTokens` ise slot serbest bırakılır + HTTP 400 (`releasePackageSlot` → failsafe). `codex-api-chatgpt` paketi 65 000 token limit ile seed'lendi.
- **`entitlement-service.ts`:** `checkPackageCoverage()` (model whitelist'te + kota dolmamış mı — salt-read), `tryReservePackageSlot()` (atomik UPDATE + packages JOIN → `max_context_tokens`/`tpm_limit`/`provider_base_url`/`provider_api_key_cipher` döner + günlük reset), `releasePackageSlot()`, `recordPackageUsage()` → `usage_records` satırı `billed_via='package'`, **cost=0** (paradan düşmez, kotadan düşer). **Seçim sırası (2026-06-19, CANLI):** `checkPackageCoverage`+`tryReservePackageSlot` artık `JOIN users` + `AND u.payg_mode=false AND e.paused=false` (PAYG hesap-modu paketi atlar→bakiye token-billing; **duraklatılmış** paket istek tüketmez, süre işler); birden çok aktif pakette `ORDER BY daily_limit_snapshot ASC, expires_at ASC` (**düşükten büyüğe** — küçük paket önce); kilit `FOR UPDATE OF e SKIP LOCKED` (yalnız entitlement, users değil). Müşteri-yönetim fn'leri: `listUserPackagesForPanel` (aktif+geçmiş, sağlayıcı/cf SIZMAZ) · `setEntitlementPaused` (sahiplik=user_id'li) · `setUserPaygMode` — "Paketlerim" sekmesi (`/api/user/packages`, `/entitlements/:id/pause|resume`, `PATCH /me {paygMode}`). **⚠️ CF gate semantiği (2026-06-20, CANLI):** `checkPackageCoverage`+`tryReservePackageSlot` günlük-sayaç koşulu artık `(e.cf_units_ordered > 0 OR e.last_reset_date < CURRENT_DATE OR e.requests_today < e.daily_limit_snapshot)` — **CF paketi (`cf_units_ordered>0`) günlük `requests_today<daily_limit` cap'inden MUAF**, yalnız CF-havuz clause'una (`cf_remaining`) göre kapılanır (CF'de `daily_limit_snapshot` GÜNLÜK değil ÖMÜRLÜK ünite sayısı; eski cap gün-içi kilitleyip panel `cf_remaining` "ünite var" gösterirken 402 veriyordu). İki gate clause LOCKSTEP düzenlenir (TOCTOU). **⚠️ CF-havuz clause'u (2026-06-23 itibarıyla):** `(e.cf_units_ordered = 0 OR e.cf_remaining IS NULL OR e.cf_remaining > 0 OR e.cf_units_ordered < e.daily_limit_snapshot)` — `OR e.cf_remaining_at < now() - interval '10 minutes'` (stale-0 supabı) **KALDIRILDI** (bkz. Canlı durum 2026-06-23 madde 4; DEADLOCK FIX zaten kapsıyor). `settleBilling` slot iadesi artık `status!=='success'` (yalnız 'error' değil → `stream_missing_usage` da release). Panel `kullanilan` = `computeDisplayConsumed` helper (SALT-GÖSTERİM, kapı/billing/cf_remaining'e DOKUNMAZ): CF mirror bayatlayınca `cf_remaining` yerine gerçek başarılı-istek sayısına düşer (`min(limit, max(cfConsumed, successCount))`). Bkz `project_yzapi_cf_gate_counter_desync` memory notu.
- **Seed scriptleri (deploy çalıştırmaz — sunucuda `NODE_ENV=production npx tsx` ile ayrı koşar, bkz env tuzağı):** `scripts/seed-gpt-istek-paketleri.ts` (canlıdaki 3 GPT paketi; ON CONFLICT `satista`/`enabled`'a BİLEREK dokunmaz — açılmış satışı geri kapatmaz); `scripts/seed-codex-package.ts`; `scripts/seed-trial-package.ts`; `scripts/seed-provider-profiles.ts` (provider upsert).
- **Canlı paketler (2026-06-18):** eski `gpt-*-1000` paketleri SİLİNDİ (CF-dışı/Popusk'tu). Yeni katalog tamamen **CF reseller** kaynaklı (`cf_catalog_id` dolu), model-ailesi kategorilerinde (CodeFast adı kaldırıldı → GPT/Codex·Gemini·GLM·Grok·Kimi·NVIDIA·Görsel·Açık Kaynak; kategori SADECE gösterim, backend cf_* ile çalışır). İçerik: 12 sabit CF paketi + **custom-fiyat matrisi** (`cf-codex-{500,1000,2000}-{1g,7g,15g,30g}`) + **builder şablonları** (`cf-*-builder`) + **free NVIDIA** (`cf-nvidia`/`-gunluk` ₺0 + per_user_once) + **redeem-only test paketi** (`cf-test-codex-50`, kategori='Deneme'). 30 paket `satista=true` (satışta); boş-modelli 6 (görsel/opensource) kapalı. **Custom builder** (Faz1-5, CANLI): müşteri Paketler'de `is_configurable` şablonda günlük limit (600–5000, adım 50) + süre (1–90g) seçer → `previewConfigurablePrice` CF-maliyet × **sabit marj** `BUILDER_MARKUP = 2.5` (`services/custom-package-pricing.ts`; `limitStepError` min **600**, 50'nin katı zorunlu — tüm tipler için server-side; eski `volumeMarkup()` tiered fonksiyon KALDIRILDI) ile anlık fiyat → bakiyeden satın alır. Geliş/marj frontend'e SIZMAZ (yalnız `fiyatTL`). `birim_tipi`: istek│kredi│lifetime(gün yok)│sabit(NVIDIA, `birim_satis_override_tl`). ⚠️ kredi tipi (görsel builder) max=500 < limitStepError min 600 → ayrı kod fix gerekene kadar `satista=false` kalır.
- **Route/UI:** `routes/packages.ts` → `/api`'ye mount (public katalog + purchase `Idempotency-Key` + `/redeem`); frontend `tab-packages.jsx` (paket grid + redeem input + aktif hak listesi: istek sayacı + progress bar + `account_delivery` için iletişim sorma; **context-limit badge'i KALDIRILDI** — limit hiçbir yerde gösterilmez).

### Frontend katalog & auth
- SPA entry zinciri: `index.html → src/main.tsx → src/App.tsx → yapayzekalab/App.jsx` (entry'ler **repo kökü/src**'de, `yapayzekalab/` içinde main.jsx/index.html YOK). **Tab dosyaları** (`yapayzekalab/tab-*.jsx`): `tab-home` (bakiye/yükleme) · `tab-ai-chat` · `tab-models` (katalog) · `tab-studio` (görsel) · `tab-packages` (paket grid+redeem+account-delivery) · `tab-mypackages` (**Paketlerim**: sahip olunan paketleri takip + duraklat/devam + kullandığın-kadar-öde modu; route `/paketlerim` — ⚠️ tab-routes'ta `/paketler`DEN ÖNCE eşlenir; auth-gerekli) · `tab-documents` (API dokümantasyonu — `api-docs.js` datasını render eder) · `tab-activity` (kullanım geçmişi) · `tab-account` (profil/güvenlik) · `tab-status` (public servis durumu) · `tab-support` · `tab-admin` + alt-tab'lar (traffic/mali-izleme/gozcu).
- **Tab routing = History API (React-Router YOK):** "sayfa"lar React state'i (`App.jsx` `const [tab, setTab]`). `yapayzekalab/tab-routes.js` tek-kaynak `tabForPath(pathname)`↔`pathForTab(tab)` (+`TAB_PATHS`) — `App.tsx` initialTab'ı buradan türetir, `App.jsx` iki effect'le senkron tutar: `useEffect([tab])` aktif tab'ı URL'e yansıtır (mount'ta `replaceState` hizala, sekme değişiminde `pushState`), `useEffect([isAuthenticated,isAdmin])` `popstate` dinler → Back/Forward SPA İÇİNDE sekmeler arası gezer (yoksa Back OAuth redirect zincirine düşüp kullanıcıyı atıyordu — `project_yzapi_panel_back_button_fix`). popstate `selectTab` guard'larını aynen uygular (admin→home / login'siz korumalı→home) ve ASLA push etmez (tarayıcı URL'i zaten güncelledi → `urlTab===tab`). ⚠️ `tab-routes.js`'e bir yol eklersen App.jsx'te o tab'ın **render branch'i olmalı** (yoksa boş sayfa); `support` BİLEREK haritada YOK (render branch'i yok → `/support`=home). Contract: `admin-single-owner-contract.test.ts` `/dashboard`→account alias'ını tab-routes.js'te bekler; `tab-routes.test.ts` round-trip invariant'ı kilitler.
- **i18n (TR/EN, sıfır-bağımlılık):** `src/yapayzekalab/i18n/` — `LangProvider` + `useT()` hook; string'ler **surface-başına fragment** dosyalarında (`strings/*.js`: app-shell, packages, ai-chat, studio, status, support…), `tr.js`/`en.js` taban sözlükleriyle merge (merge-conflict azaltır). Aktif dil: `localStorage[yz_lang]` → `navigator.language` → `tr`; kullanıcı tercihi `users.lang` kolonunda (migration `0023`). Backend de lokalize: `requestLang` middleware `req.lang` çözer (lokalize API hataları).
- `shared.jsx` ES module `export {}` blokları kullanır (default export YOK): icons (I), components (Card/Chip/Caption/PulseDot), data (PROVIDERS/MODELS), helpers (modelMeta/fmt/useCountUp). Müşteri katalog **dinamik** `/api/models`; `shared.jsx` MODELS = STATIK fallback (42-model contract testi kilitler). `legal-docs.js` KVKK/gizlilik metinleri, `auth-client.js` JWT/refresh yardımcıları.
- **Fiyat karşılaştırması "üretici fiyatı vs YapayZekaLab" (CANLI 2026-06-20, salt-pazarlama/gösterim):** Yeni saf modül **`yapayzekalab/catalog-pricing.js`** (React'siz, testli `catalog-pricing.test.ts`): `CATALOG_TIERS`/`CATALOG_PRICES` = modeli ÜRETEN şirketin **resmi liste fiyatı** (USD/1M in/out; her biri **resmi sayfadan** doğrulandı — docs.claude.com/openai.com/ai.google.dev; **Opus 4.7/4.6/4.8 = Fast mode** tarifesi $30/$150·$10/$50, `FAST_MODE_IDS` etikete "(Fast mode)" ekler; GPT-5.5 $5/$30 vb.) + `computeCatalogDiff(model)` (toplam=in+out; **biz ucuz DEĞİLSEK veya katalog yoksa null** → asla yanlış/ters kıyas). `shared.jsx` bunları re-export eder + `providerLabelFor()` ("Anthropic/OpenAI/Google fiyatı"). Tüketenler: **`tab-models.jsx`** ("Üretici fiyatı" sütunu kırmızı/çizili + YapayZekaLab + −% rozet; eski "iki sütun aynı fiyat" bug'ı kapandı) ve **`tab-home.jsx`** — hero `RouteFlowPrices` (eski `RouteFlow` SVG akışı + model düğümleri artık fiyat kartı; **hibrit**: sabit 148px SVG rail + HTML kartlar) + Değer Banner üstü 6-model `PriceComparison`; ikisi de animasyonlu (`useCountUp` + CSS bar dolum + stagger + `CMP_REDUCE` reduced-motion guard). ⚠️ **`CMP_SYNTHETIC`/`cmpModel()`**: 42-kilitte OLMAYAN modeli (örn. `claude-opus-4.8`, ≈$1.10/1M) öne çıkarmak için tab-home içi sentetik girdi (shared.jsx MODELS'e EKLEME yok → kilit korunur). Fiyatlar SALT gösterim — billing/routing'e dokunmaz; nano gibi bizden ucuz modeller otomatik gizli ("—"). Detay: `project_yzapi_price_comparison` memory notu.
- **`api-docs.js` + `tab-documents.jsx` (docs DATA/RENDERER split):** `api-docs.js` veri: `API_DOC_SECTIONS` (klasik referans — `clientCards`/`referenceRows`/`modelGroups`/`codeBlocks`/`osVariants:{windows,macos,linux}`/`desktopCode`) **+ `CLIENT_GUIDES`** (yeni, 2026-06-18 CANLI). `tab-documents.jsx` artık bir **HUB** olarak açılır (`view` state: `'hub'` | `<guide.id>` | `'classic'`): üstte "Hangi aracı kullanıyorsun?" araç kartları → birine tıklayınca SADECE o aracın adım-adım **görselli** sayfası (`ClientGuidePage`); "Tüm araçlar + API referansı" kartı eski uzun içeriği (`'classic'`, TOC+`API_DOC_SECTIONS`) açar. Görseller kod-içi **mockup** (`GuideVisual`: `app`/`browser`/`file`/`chat`/`errors` — gerçek/sahte ekran görüntüsü DEĞİL, `public/docs/*.png` YOK). Hazır rehberler: **Codex masaüstü** (config.toml, OS-sekmeli) + **Claude masaüstü** (Developer Mode→Configure Third-Party Inference, x-api-key). Giriş yapan müşterinin `yzk_live_` anahtarı `ApiKeyBox`'ta kopyalanabilir + `pk()` ile mockup/koda gömülür (`reveal ? myKey : null`; çıkışta yer-tutucu). Adım `osVariants:{macos,windows}` varsa `OsTabs` toggle + OS-özel kopyalanabilir blok (Codex config'i eski `config.toml`/`auth.json`'u `.bak`'a yedekleyip temizler). **Contract testler klasik yüzeyi kilitler** (`documents-content-contract` + `os-install-variants-contract`) — `API_DOC_SECTIONS` güncellenince her ikisi PASS kalmalı (CLIENT_GUIDES contract-kapsamı dışı). Kalan: Claude Code/Cline/Roo/OpenAI hâlâ klasik sayfada (görselli sayfaya çevrilebilir).
- `tab-admin.jsx`: sections **module-level `const ADMIN_SECTIONS = [...]`** array'i (register() çağrısı değil), `.map()` ile render. Tüm sekmeler (ALL_TABS sırası): dashboard, traffic, mali-izleme, gozcu, api, users, overrides, packages, codes, teslimler, announce, providers, kur, payments, telegram, apikeys, logs, animations. Partner'a açık 11'i: dashboard/traffic/mali-izleme/gozcu/announce/payments/telegram/apikeys/logs/animations/users (api/overrides/packages/codes/teslimler/providers/kur owner-only). `const API_SUB_SECTIONS` API-yönetim alt-tab'larını sürer. Sub-tab'lar `tab-admin-traffic.jsx` / `-mali-izleme.jsx` / `-gozcu.jsx`'ten gelir. (Hardcoded `LAUNCH_ADMIN_EMAIL='cix.crazy666@gmail.com'`.)

- **Temalama & dark mode (`tokens.css` + `App.jsx`):** Tüm UI renkleri `tokens.css` CSS değişkenlerinden gelir (`--bg`/`--surface`/`--surface-2`/`--ink`(-2..5)/`--border`(-st)/`--accent*`/`--ok*`/gölgeler). Tema `<html data-theme>` + `data-accent` ile: **3 AÇIK** tema (`original` varsayılan/`soft`/`studio`) + 4 accent (`blue`/`mint`/`peach`/`lav`) + **1 KOYU** (`dark`, 2026-06-27 CANLI). `App.jsx` `useEffect` → `dataset.theme = darkMode ? 'dark' : t.theme`. **Dark mode toggle** (üst bar 🌙/☀️): `darkMode` state, localStorage `yz_theme` (`dark`|`light`), yoksa `prefers-color-scheme`. ⚠️ **Özgüllük tuzağı:** dark blok `html[data-theme="dark"]{...}` (özgüllük 0,1,1) ile yazılır — tek-attr `[data-accent="..."]` blokları accent değişkenlerini dark'tan SONRA set ettiğinden, daha düşük özgüllük olsa accent tonları dark'ta açık kalırdı. ⚠️ **GOTCHA — hardcoded renk dark'ı kırar:** bir bileşen `var(--...)` yerine sabit renk kodlarsa (`background:'white'`, `color:'#0f172a'`, `#fff`, `rgba(15,23,42,..)`) dark modda kırılır (beyaz kart / görünmez metin) → yeni UI'da HER ZAMAN `var(--...)` kullan. İstisna: bilinçli sabit-koyu öğeler (kod blokları `#0f172a`, `linear-gradient(#0f172a,#1e293b)` butonlar — iki modda da doğru); `var(--ink)` zeminli butonda metin `#fff` yerine `var(--bg)` olmalı (iki modda görünür). Docs hub'ı için yeni `docs-landing.jsx` (`RedesignedDocsLanding`).

### Job'lar (`src/server/jobs/index.ts → startAllJobs`)
**9 job** (sıra ÖNEMLİ, hepsi NODE_ENV==='test'te skip — telegram-recovery hariç):
| Job | Cron | İş |
|-----|------|-----|
| kur-refresh | `0 * * * *` | saatlik USD/TRY + >5% anomali email |
| low-balance-scan | `15 * * * *` | saatlik düşük-bakiye email (24h cooldown) |
| daily-report | `0 9 * * *` | günlük kullanım/gelir/top-5 model |
| telegram-delivery-recovery | `*/10 * * * *` | başarısız API-key teslimat retry (attempt<5) |
| orphan-reservation-reaper | `30 * * * *` | sahipsiz `usage_reserve_*` hold iade |
| mali-izleme | `* * * * *` | activity-gated finansal denetim → `mali_izleme_taramalari` |
| **gozcu-job** | `* * * * *` | Gözcü scan + auto-heal + red-notify |
| **gozcu-digest** | `0 9 * * *` | günlük Gözcü bulgu özeti |
| package-maintenance | `5 0 * * *` | süresi dolan aktif entitlement'ları `status='expired'` yapar (para taşımaz; günlük kota reset'i ayrı/lazy) |

Sonda `markJobsStarted()` heartbeat staleness için boot anını kaydeder.

### Gözcü (Sentinel) — sistem nöbetçisi (`services/gozcu/`)
**READ-ONLY** sinyal tabloları üzerinde (yalnız SELECT); yalnızca `gozcu_findings` + `gozcu_signals` yazar — **parayı asla taşımaz**. Mali İzleme'nin `applyHysteresis` + `deriveVerdict` primitiflerini yeniden kullanır.
- **5 domain** (`registry.ts CHECK_REGISTRY`): **money** (Mali İzleme'nin 11 kontrolü / 14 alt-sonucu; k1 + ledger_drift≥1TL "immediate"), **uptime** (api_5xx, proxy_error, p95, job_liveness, db_pool_health, unhandled_error_spike), **provider** (upstream_error/timeout, **models_reachability** — aktif provider /models probe'u, ≥3 fail→red + failover suggestedHeal, provider_durum_staleness), **security** (auth_failure_spike, rate_limit_hit_spike, shopier_dead_letter, pending_iban, signup_bonus_abuse, telegram_delivery), **code** (`code_contracts` → verifyAllContracts, runtime'dan bağımsız regresyon yakalar).
- **Engine** (`engine.ts`): `runGozcuScan()` 5 domain'i paralel `safeRun` ile koşar (throwing check → yellow 'check_failed', engine çökmez), hysteresis + verdict, `persistScan()` dedup_key (domain:name) ile upsert + PII redaction (scrubObject), `gozcu_signals` özet satırı. `runGozcuScanIfActivity()` activity gate'i (para/trafik/stale heartbeat varsa veya en az 10dk'da bir zorla scan).
- **Metrics-collector** (`metrics-collector.ts`): in-memory 15×1dk ring buffer (app.ts `res.on('finish')` + middleware besler). Hot path'te DB yazımı YOK; veri yalnız scan anında (dk/1) `gozcu_signals`'a girer. Ham IP saklanmaz (sha256 `hashIp`, IP_CAP=200/bucket).
- **Heartbeat** (`heartbeat.ts`): cron başında `recordHeartbeat(jobName)`; "process canlı ama cron sessizce durdu"yu yakalar (mali-izleme + gozcu izlenir). Total process ölümü → EXTERNAL `scripts/gozcu-heartbeat.mjs`.
- **Layer-2 LLM diagnosis** (`diagnose.ts`, **advisory-only**): red bulgu için redacted code slice + contract → upstream /chat/completions (triage→deep escalation if confidence<0.6 veya kritik). TEXT-ONLY (rootCause/suggestedFix/confidence), DB/tool erişimi yok, heal tetiklemez, monthly token budget + 6h cooldown, asla throw etmez. Default modeller (env): `GOZCU_LLM_TRIAGE_MODEL='antigravity/gemini-3-flash-preview'`, `GOZCU_LLM_DEEP_MODEL='antigravity/claude-opus-4-6-thinking'` (kod içi "Haiku/Opus" yorumu stale).
- **Layer-3 code contracts** (`contracts/index.ts`): kodun NE OLMASI gerektiğinin declarative kaydı. `proxy/billed-tokens-floor` verify() proxy.ts'te `resolveBilledPromptTokens`'ı grepler, **<4 occurrence → violation** (billing-floor regresyonunu yakalar; okunamayan dosya = violation). Diğer: `billing/error-never-charged`, `billing/reserve-settle-or-release`, `payments/idempotent-credit`.
- **Auto-heal** (`heal/run.ts`, **default off**): `GOZCU_AUTOHEAL_MODE` (off/shadow/tiered/auto). HEAL_REGISTRY **3 whitelist**: `refund_orphan_reservations` (idempotent reap, yeni bakiye yazmaz), `expire_stale_pending_iban`, `failover_provider` (active_provider_id'yi erişilebilir alternatife çevirir). Her apply rails: rate-limit → guard → dryRun → cap → apply → audit.
- **Notify** (`notify.ts`): yalnız red, dedup_key başına 1h cooldown; hysteresis non-immediate red'leri yellow'a indirir → yalnız immediate/para-kritik page eder (k1, drift≥1TL, DB down, mali-job stall, models down, auth attack). `noteRecoveries()` green'de cooldown'u siler. Yellow'lar günlük digest'e.

---

## Kritik Kararlar & DOKUNULMAZ

**DOKUNULMAZ (regresyon / para riski — onaysız değiştirme):**
- `billing-service.ts` reserve/settle/charge transaction mantığı (atomik; K1 hata→0 tahsil; idempotency key'leri)
- `resolveBilledPromptTokens` + `normalizeProviderUsage` — token **SAYISI** mantığı, billing **FORMÜLÜ DEĞİL**
- Tek-admin allowlist (`admin-auth.ts` ADMIN_EMAIL kod-sabiti + `admin.ts`) — contract testleri kilitler
- `MASTER_MODELS` 42-kilit — yeni model `added_models`'a; `reject-template-guard` fingerprint listesi
- secret/codename non-leak: provider adı/base_url/upstream maliyet/çarpan asla frontend'e/public API'ye sızmaz
- `API_KEY_ENCRYPTION_SECRET` (prod'da zorunlu + JWT_SECRET'tan ayrı olmalı — JWT_SECRET sızması her API key'i çözmesin)

| Konu | Karar |
|------|-------|
| Fiyatlama (USD→TL) | `pricing.ts`: tip-bazlı carpan (default **3.0**) → `toTL(usd,cfg) = usd * (liveKur * (1 + kurBuffer))`. ⚠️ **SELL kur** kullanılır (`liveKur*(1+kurBuffer)`, default kurBuffer 0.03) — depolanan `kur` kolonu DEĞİL. Müşteri fiyatları `master-models.ts` + `added_models`. CLAUDE.md tarihsel "~3.33×" ifadesi normalizasyon dahil efektif değer. |
| Cost (token→para) | `computeCost()`: `TOKEN_PRICE_UNIT=1e6`; text = `(prompt/1e6)*inputPrice + (completion/1e6)*outputPrice`; round4 TL / round8 USD. |
| Token normalizasyon | **`/0.90` divizörü YOKTUR** — aktif kodda böyle bir sabit yok (repodaki tek `0.90` seed.ts'te bir model fiyatı). `normalizeProviderUsage()` cache-aware toplama yapar; CLAUDE.md'nin eski "real_tokens / 0.90" satırı stale/aspirasyonel idi. |
| Billing idempotency | `usage_records.request_id` UNIQUE (kolon nullable, index tekil); `transactions.idempotencyKey` UNIQUE. Key'ler: `usage_reserve_` / `usage_release_` / `usage_final_` / `usage_<reqId>`. |
| Admin erişimi | Allowlisted email **kod-sabiti** (`ADMIN_EMAIL='cix.crazy666@gmail.com'`), ayrı şifre yok |
| Min yükleme | **$5 USD-bazlı** — backend `payments.ts MIN_TOPUP_USD=5` (Shopier/IBAN/crypto tek kapı `buildQuoteFromRequest`) + frontend `tab-account.jsx MIN_USD=5`; Telegram TL tabanı `system_config.min_bakiye_tl=225`. `payment-safety-contract.test.ts` backend+frontend sabitlerini KİLİTLER — değişiklik üçünü (+`home.js` $ metinleri) birlikte günceller |
| Provider leak | codename'ler (closerouter/wellflow/omniroute/metro/popusk) frontend'e ASLA geçmez — `npm run scan:public` (BUILT dist) + `*-noleak`/`*-contract` testleri (`provider-name-noleak-contract`, `catalog-noleak`, `provider-config-noleak`) + vite.config.ts `reject-template-guard` (SOURCE-time gate, scan:public bundle-time gate) |
| Migration | **CANLI highest `0051_cf_topup_guard.sql`** (0047 sonrası: `0048_cf_remaining_numeric` · `0049_claude_cf_override` · `0050_codex_rolling_window` · `0051_cf_topup_guard`). ⚠️ Lokal `~/yzapi` `0041`'de GERİDE — yeni migration numarasını **canlı sıraya göre** ver (CANLI'dan `ls migrations/` ile teyit et). Eski referans: `0000…0047` (highest `0047_guard_events_2.sql`). Yeni (2026-06-19): `0040_package_pause_payg` (`user_package_entitlements.paused` + `users.payg_mode` — Paketlerim duraklat / kullandığın-kadar-öde; CANLI) · `0038_entitlement_cf_remaining` (`user_package_entitlements.cf_remaining`/`cf_remaining_at`/`cf_units_ordered` — CF mirror + lazy provisioning; CANLI) · `0039_token_saver` (`system_api_config.token_saver_enabled`; CANLI). (2026-06-18): `0034_codefast_template_id` · `0035_custom_package_builder` (`packages.cf_unit_cost_tl`/`birim_tipi`/`birim_satis_override_tl`) · `0036_redeem_code_copied` (`redeem_codes.copied_at`) · `0037_package_per_user_once` (`packages.per_user_once`). Eskiler: Önemli (numaralar DOSYA ADLARIYLA doğrulandı 2026-06-11): `0017_gozcu` · `0019_provider_fallback` (`provider_profiles.fallback_provider_id`) · `0020_packages` · `0021_redeem_codes` · `0022_added_model_image` · `0023_github_oauth_account_delivery` · `0024_user_lang` · `0025_user_role` (partner RBAC) · `0026_configurable_packages` · `0027_package_context_limit` · `0028_usage_provider_profile` (`usage_records.provider_profile_id` text) · `0029_package_tpm_limit` · `0030_package_satista` · `0031_min_topup_5usd` (`min_bakiye_tl`→225) · `0032_package_provider_override` · `0033_codefast_reseller` (`packages.cf_*` + `user_package_entitlements.cf_*` CodeFast metadata; CANLI/uygulandı). ⚠️ İzole deploy için migration numarasını canlı sıraya göre ver. Yeni tablo: `IF NOT EXISTS` + `schema.ts` Drizzle + `meta/_journal.json` sıralı; deploy `db:migrate` koşar. ⚠️ **`meta/_journal.json` `when` MUTLAKA mevcut max'tan BÜYÜK olmalı — kozmetik DEĞİL:** drizzle migrator uygulanacakları `when`-sırasına göre seçer; yeni girdinin `when`'i mevcut max'tan KÜÇÜKSE migration'ı **SESSİZCE ATLAR** (`db:migrate` yine "Migrations applied successfully" basar ama kolonlar OLUŞMAZ → deployed kod var-olmayan kolona yazıp her ilgili istek kırılır). Migration üretecinin/`node -e Date.now()`'ın değeri bazı ortamlarda mevcut `when`'lerden KÜÇÜK çıkabilir (whenler ~gün-artımlı sabitlerle ilerliyor). **DEPLOY SONRASI kolon/index'in gerçekten var olduğunu `information_schema.columns`/`pg_indexes` ile DOĞRULA — "applied successfully" çıktısına GÜVENME.** Atlandıysa kurtarma: idempotent `ALTER ... IF NOT EXISTS` elle çalıştır + journal `when`'i max+1'e çek + yeniden `db:migrate` (drizzle no-op uygulayıp `__drizzle_migrations`'a yazar). (2026-06-20 `0041_purchase_ref` tam bu yüzden atlandı, böyle kurtarıldı; `0041_purchase_ref` + `0042_cf_usage_ledger` CANLI 2026-06-20.) |
| itest | gerçek Postgres (mock yok); `fileParallelism: false`; `itest-setup.ts` (mock-DB DEĞİL, real `./db/client`) |

### Raw-SQL Date param tuzağı (GOTCHA)
Raw `sql`/`dbSql` template literal'a doğrudan JS `Date` geçmek **runtime'da throw eder**: `TypeError: Received an instance of Date`. Önce serialize et: `${date.toISOString()}::timestamptz`. Pattern `mali-izleme-service.ts` `hasActivitySince()`, `gozcu/engine.ts`, `user-usage-stats-service.ts`'te. ⚠️ Bu yalnız **raw dbSql interpolasyonu** için — Drizzle'ın typed `gte()/lte()/eq()` operatörleri Date object'i sorunsuz kabul eder.

### DB client (`db/client.ts`)
Tek postgres-js pool (`max:10`). İki export aynı pool'u paylaşır: **`db`** = `drizzle(sql, {schema})` typed ORM; **`dbSql`** = raw postgres client (template-literal SQL, `dbSql.begin` transaction, limitless aggregate).

### env (`lib/env.ts`)
Tek env erişim noktası (Zod-validated `env`); doğrudan `process.env` kullanma. Import'ta `dotenv.config({path: NODE_ENV==='production' ? '.env.production' : '.env'})` + bare `dotenv.config()` fallback → sadece `NODE_ENV=production` set etmek `.env.production` okutur. Prod-only superRefine: `API_KEY_ENCRYPTION_SECRET` zorunlu + JWT_SECRET'tan farklı; WHATSAPP_OTP açıksa `WHATSAPP_OTP_HASH_SECRET` zorunlu + farklı (dev/test'te JWT_SECRET'a fallback). PORT default 4567, HOST 127.0.0.1 (loopback, yalnız nginx üzerinden).

---

## Test Katmanları
- **Unit (`*.test.ts`):** `npm test` (`vitest run`, `vitest.config.ts`) — DB-mock'lu/saf logic; setup `src/server/__tests__/setup.ts`
- **Contract (`*-contract.test.ts`):** bundle/sabit bütünlüğü (provider leak, fiyat parity, 42-lock, frontend display order, api-docs içerik). ⚠️ Dokümantasyon yüzeyi (`yapayzekalab/api-docs.js` + `tab-documents.jsx`) iki testle string/yapı-kilitli: `documents-content-contract` (zorunlu metinler: endpoint'ler, istemci adları, `ANTHROPIC_AUTH_TOKEN`, "Kod kopyala"/"Kopyalandı", TOC fn'leri) + `os-install-variants-contract` (her `osVariants` win/mac/linux dolu; Claude Code `$env:`/`setx`/`ANTHROPIC_AUTH_TOKEN`; Windows curl tek-satır `curl.exe`; her blokta `yzk_live_YOUR_KEY` placeholder). Dokümanı düzenlerken bunları koru.
- **Integration (`*.itest.ts`):** `npm run itest` (`--config vitest.itest.config.ts`) — gerçek Postgres (`db:up` + `db:migrate`); `fileParallelism:false`, timeout 30s. ⚠️ **`.pgdata` tuzağı:** lokal PG verisi bind-mount `./.pgdata`'da — `docker compose down -v` bunu SİLMEZ; bayat .pgdata (migration-renumber kalıntısı) `column "is_configurable" does not exist` tarzı TOPLU itest hatası üretir. Çözüm: `docker rm -f yzapi-postgres-1 && rm -rf .pgdata && npm run db:up`. Taze volume'de initdb sürer → migrate öncesi `docker exec yzapi-postgres-1 pg_isready -U yzapi -d yzapi` bekle (yoksa ECONNREFUSED). İlk itest koşusu ara sıra 1 test flake'ler — tekrar koş.
- **E2E:** `npm run e2e` (playwright chromium); setup `npm run e2e:up`
- **Smoke (`scripts/`):** canlı/local prod build'e HTTP probe (`smoke:vps`, `qa:uat`, `preflight:live`)

## Önemli Notlar
- Tüm env erişimi `src/server/lib/env.ts` (Zod) üzerinden; doğrudan `process.env` kullanma
- `scan:public` BUILT bundle'ı (dist/) tarar → `npm run build` SONRASI, deploy/commit ÖNCESI koş
- Money-critical tablolar: `users` (bakiyeTL numeric(14,4)), `transactions` (ledger, idempotencyKey UNIQUE), `usage_records` (costTL/costUsd, requestId UNIQUE; `billed_via`+`entitlement_id`), `payments`, `pendingIbanPayments`, `signupBonusGrants`, `systemConfig` (single-row id=1: kur/liveKur/kurBuffer + text/image/videoCarpan + `packages_enabled`), `packages`/`userPackageEntitlements`/`redeemCodes`/`redeemCodeUses`/`accountDeliveryOrders` (entitlement & kod akışı — `billed_via='package'` cost=0)
- **Canlı durum (2026-06-27):** **(1) Site-geneli açılıp-kapanır DARK MODE CANLI** (izole 3-dosya `tokens.css`+`App.jsx`+`tab-packages.jsx` targeted-rsync, 3/3 QA, sunucu gate `lint→test 1287/1287→build→scan→restart health 200`; yedek `.deploy/darkmode-backup-20260627T103327Z`; bkz `project_yzapi_dark_mode` memory notu): `tokens.css` `html[data-theme="dark"]` koyu slate + üst bar 🌙/☀️ toggle (localStorage `yz_theme` / `prefers-color-scheme`); `tab-packages.jsx` light-locked renkler `var(--...)`'a çevrildi (bkz Frontend → "Temalama & dark mode" gotcha'sı). FAST-FOLLOW: hesap/paketlerim/ai-chat (giriş-arkası, çıkışken doğrulanamadı) birkaç hardcoded renk dark'ta hâlâ açık → bir sonraki turda. ⚠️ targeted rsync → manifest GÜNCELLENMEDİ; lokal `main`'de YOK. **(2)** `/documents` yeni tasarımı (HUB → araç-başı ok-işaretli detaylı görselli kurulum sayfası + KALICI KURULUM blokları + "Tüm araçlar + API referansı" altta) **standalone mockup olarak ONAYLI** (`~/yzapi-documents-mockup.html` + `-reference.html` + `-codex-guide-demo.html`) ama **canlı React port'u YAPILMADI** = sıradaki deploy; bkz `project_yzapi_documents_redesign` memory notu. (Not: mevcut canlı `/documents` v1 hâlâ `docs-landing.jsx`/`tab-documents.jsx`.)
- **Canlı durum (2026-06-25):** **Devreden (rollover) CF Codex paketleri CANLI + SATIŞTA** (izole **17-dosya** deploy `rsync --checksum --files-from` + sunucu gate `lint→test 1056/1056→build→migrate`→restart health 200; **subagent-driven TDD 8 faz, her görev opus-review + final whole-branch review**; yedek `.deploy/devreden-backup-20260625T062901`; bkz `project_yzapi_devreden_paketler` memory notu). Ürün: müşteri **günlük istek kotası** alır, kullanılmayan ertesi günlere **devreder** (sınırsız, **50-hizalı**, küsürat yanar) + **saatlik 150 istek** hız freni (DB-backed); CF Codex/GPT (gpt-5.5/5.4/5.2); **Kendin Yap builder** (100–10.000 istek × 2–30 gün; kilitli **lookup-tablo** fiyatı `devreden-pricing.ts`, `roundClean` BYPASS, bilinear interp) + **12 sabit paket** (500/1000/2000 × 1/7/14/30). Fiyat çapaları 100/1g=40, 500/1g=169, **1000/30g=8750**, giriş 100/30g=1059; min marj 4.14×; model `gunlukFiyat(n)=7.75+0.3225n × durationFactorB(d)=1−0.11683(d−1)/29`, order-preserving yuvarlama. **Migration `0045_package_rollover`** (7 kolon: `packages.devreden`/`saatlik_limit` + `user_package_entitlements.daily_quota`/`rollover_balance`/`saatlik_limit`/`hour_window_start`/`requests_this_hour` + CHECK `rollover_balance>=0 AND %50=0`; hepsi nullable/default = mevcut paketlere INERT). Gate (`checkPackageCoverage`+`tryReservePackageSlot` LOCKSTEP, R-3/shared-pool clause'larına DOKUNMADAN AND eklendi): günlük-tavan `requests_today < daily_quota + rollover_balance` + saatlik clause; ORDER BY `COALESCE(e.daily_quota, e.daily_limit_snapshot) ASC` (devreden donmaz). Devir birikim **mevcut atomik reset-UPDATE'in SET CASE'ine GÖMÜLÜ** (`rollover_balance += floor(max(0,daily_quota−requests_today)/50)*50 + (gap−1)*daily_quota`, GREATEST(0) clamp → over-serve'de negatif olmaz, çift-birikim race kapanır). DB-backed saatlik (`requests_this_hour`/`hour_window_start`; `releasePackageSlot` saatlik iade simetrik; `setEntitlementPaused` resume'da accrual tabanını sıfırlar = paused gün birikmez). proxy 3 text call-site `checkHourlyExceeded`→`RateLimitError 429`+Retry-After (rezervasyondan ÖNCE, **billing-neutral** — early-return settleBilling'i atlar). `grantPackageEntitlement` devreden-aware (`daily_limit_snapshot=daily_quota×sure_gun`, her alım AYRI satır — EXTEND yok). Panel + frontend rozetler (🔄 Devreden / ⏱ Saatlik 150), `publicShape` `devreden`/`saatlikLimit` expose (cf_*/provider/cost SIZMAZ). **Para yolu (billing reserve/settle/charge) DOKUNULMADI** (diff boş). ⚠️ **MID-SESSION MIGRATION ÇAKIŞMASI (deploy-prep'te yakalandı, kritik ders):** replika pull-SONRASI canlıya başka iş kolu `0044_lord_action_flag` (`system_api_config.lord_action_enabled`, Lord-Brain INERT switch) eklemiş → benim migration **0044→`0045_package_rollover`'a rebase** (journal `when` 1783200000000 > canlı 0044), `schema.ts`'e `lordActionEnabled` eklenip canlının değişikliği KORUNDU. **DERS: deploy-prep'te HER ZAMAN canlı migration max'ı + ayrışık gate dosyalarının md5'ini baseline'la kıyasla (canlı seans-içi drift edebilir); çakışırsa migration'ı renumber + schema/journal'ı canlıya rebase.** Geliştirme **canlı-faithful replikada** (`~/yzapi-devreden`; lokal `main` R-3/shared-pool/lord_action'dan GERİDE). Satışı kapat: `UPDATE packages SET satista=false WHERE devreden=true`. ⚠️ targeted rsync → **manifest GÜNCELLENMEDİ**. Residual: `cf_unit_cost=0.069` per-istek-TL teyidi, limitStepError %50-mesajı "600" örneği + 429 mesajı sabit "150" (kozmetik), `daily_quota%50` DB-CHECK. **(2) messages `user` field fix CANLI** (izole 1-dosya `proxy.ts` rsync, gate 1063/1063, restart 13:24:53Z): gateway `/v1/messages` body'sine OpenAI `user` alanını inject ediyordu → Anthropic API `400 "Extra inputs are not permitted"` → Claude tamamen down (cliproxy/Vertex her ikisi de reddeder). Fix: `user` yalnız chat/responses'ta, messages endpoint'inde body'den çıkarılır. Yedek `.deploy/user-field-fix-backup-20260625T162208Z`. **(3) CF over-serve cap fix CANLI** (izole 2-dosya `proxy.ts`+`cf-overserve-cap.ts` rsync, gate 1066/1066): GPT/codex paketi koltuk (`sub-codex`) üzerinden gittiğinde `cf_remaining` düşmez → eski `shouldCapOverServe` `cf_remaining>0` → "kota var" exemption → SINIRSIZ servis (yokum 936/500). Fix: `shouldCapOverServe` artık mirror-bağımsız `cfServed > limit × capMult` kontrolü (CF-servis yolundan bağımsız). `cf-overserve-cap.ts` yeni izole dosya. **(4) Request guardrails CANLI (HEPSİ OFF=inert):** OmniRoute-tarzı içerik-engelleyiciler 5 modül — harness+jailbreak+PII+NSFW+policy — canlıda ama `guard_*_mode='off'` (migration 0046/0047 `guard_events` audit tablosu). Feature branch `~/yzapi-guardrails-work` (lokal `main`'e MERGE EDİLMEDİ). Aktivasyon insan kararı: `UPDATE system_api_config SET guard_jailbreak_mode='log'` vb. **(5) ⚠️ LOKAL MAIN DURUMU:** `~/yzapi` (lokal main) canlıdan ÇOK GERİDE — şu an live'da olup lokalde olmayan özellikler: R-3 `shouldCapOverServe`, shared-pool gate fix, lord_action flag, devreden paketler, user-field fix, cf-overserve-cap.ts, guardrails (0046/0047), `seatPrimaryPackageChain`/sub-codex, ⚠️ SEAT-FAST-MODE-PROBE (`proxy.ts`'te **GEÇİCİ TEŞHİS LOGU** — fast mode sinyali tespit edilince veya araştırma tamamlanınca kaldırılacak). **`LOCAL_SRC=~/yzapi bash scripts/sync-deploy.sh` YASAK** — canlıyı geri alır. Her deploy: `scp yzapi-vps:/opt/turkapiprojesi/<dosya> /tmp/live-<dosya>` + hunk uygula + `rsync --checksum` ile sadece değişen dosyaları kapat.
- **Canlı durum (2026-06-23):** **(1) Paket fiyatlandırma yeniden yapılandırması** (`custom-package-pricing.ts`+test; izole 2-dosya rsync, 3/3 QA): `volumeMarkup()` kademeli (2.5→1.5×) fonksiyonu kaldırıldı → sabit `BUILDER_MARKUP = 2.5` sabiti (139 TL/500 istek çıpası); `limitStepError` min 50→**600** (istek), adım 50; `roundClean` <200→10'a, <2000→25'e, ≥2000→50'ye. GPT/Codex 12 sabit paketin fiyatları DB'de yeniden set edildi (500/1g=139, 1k/1g=275, 2k/1g=550; 7g %3, 15g %5, 30g %7 indirim; marj min %57). Satışa kapalı paketler (Gemini, Composer/Grok, Kimi) aynı — CF upstream kırık, müşterilere iade yapıldı. **(2) `tab-packages.jsx` tamamen yeniden tasarlandı** (`dist/assets/index-LKavqbm8.js`; izole 3-dosya rsync, restart YOK, 3/3 QA, 922/922 test): yeni `CAT_BADGE` sistemi (beyaz kart, renkli kategori rozeti per-kategori); **çift filtre** (Model + Süre chip'leri — mor `#4f46e5` aktif); **senaryo pilleri** (Günlük Test/Haftalık Proje/Aylık Plan/Görsel Üretim → ilgili filtreleri set eder); sıralama dropdown (fiyat/birim fiyat); birim fiyat (₺/istek-gün) gösterimi; `fiyatUsd>0` değilse TL fiyatı göster (sıfır-gösterim bug fix). Eski bundle `index-BvCc0-HB.js` yedekte `.deploy/pkg-ui-backup/`. ⚠️ targeted rsync → **manifest GÜNCELLENMEDİ**. **(3) Builder paketler satışa açıldı** (DB-only, deploy YOK, 3/3 QA): `cf-codex-builder` / `cf-glm-builder` / `cf-grok-builder` / `cf-nvidia-builder` / `cf-opensource-builder` → `satista=true` + `min_gunluk_istek=600` (DB'de 50 yazıyordu; `limitStepError` server-side 600 zorunlu kılıyor; arayüz 50 gösterip server 400 dönerdi). Kapalı kalan: `cf-gemini-builder` (Gemini %100 502) · `cf-composer-builder` (Composer bozuk) · `cf-kimi-k27-code-builder` (CF TARGET_SECRET eksik) · `cf-gpt-image-2-builder`/`cf-grok-imagine-builder` (kredi tipi, max_gunluk_istek=500 < limitStepError min 600 → kod fix gerekiyor) · `cf-codex-hediye` (hediye, unit cost yok). **(4) CF gate stale-0 supabı KALDIRILDI** (`entitlement-service.ts`, izole canlı-hunk deploy + `npm run build` + restart; lokal src de güncellendi): kök-sorun x1siberkampus1x, paket yenilemiş ama 402 "Paket kotanız doldu (CF)" alıyordu. Tükenmiş Kendin Yap (cf_remaining=0, cf_remaining_at=33dk ÖNCE) stale-0 supabına (`OR e.cf_remaining_at < now() - interval '10 minutes'`) takılmış, ORDER BY daily_limit_snapshot ASC sırasında 300<500 → ÖNCE seçilmiş, ardından R-3 `shouldCapOverServe` (cfServed=361 ≥ 300×1.2=360) → 402. Taze 500/gün hiç ulaşılmadı. **Fix:** her iki gate fn'inden (`checkPackageCoverage` + `tryReservePackageSlot`) stale-0 supabı clause'u çıkarıldı; yerine DEADLOCK FIX `(e.cf_units_ordered < e.daily_limit_snapshot)` zaten headroom paketleri kapsıyor (tükenmiş paket için cf_units_ordered = daily_limit_snapshot → deadlock clause FALSE, tükenmiş doğru elenir). Acil DB fix: x1siber tükenmiş entitlement `cf_remaining_at = now()` UPDATE (supabı geçişi kesti, istek anında 500/gün seçildi). Yedek: `.deploy/entitlement-service-stale-supab-backup-*.ts`. ⚠️ **R-3 / shouldCapOverServe CANLI-LOKAL AYRIŞMASI:** R-3 (92206f1, `cf_overserve_cap_multiplier=1.2`) canlı `dist/server.js`'te VAR ama lokal `main`'de YOK — `proxy.ts` `shouldCapOverServe` + `entitlement-service.ts` RETURNING `upe.cf_served, upe.daily_limit_snapshot, upe.cf_remaining, upe.cf_remaining_at` + `PackageCoverage` arayüzündeki bu 4 alan lokal main'de TANIMLI DEĞİL. **`LOCAL_SRC=~/yzapi bash scripts/sync-deploy.sh` YASAK** (R-3 kırılır). `entitlement-service.ts` değişikliği için her seferinde VPS'ten canlı dosyayı indir (`scp yzapi-vps:/opt/turkapiprojesi/src/server/services/entitlement-service.ts /tmp/live-es.ts`), hunk uygula, canlı-tabanlı versiyonu rsync et. **(5) CF PAYLAŞIMLI-HAVUZ GATE FIX** (`entitlement-service.ts` `updateCfRemaining` + `codefast-provisioning-service.ts` `topUpCfIfNeeded` + `proxy.ts` settleBilling wiring + 2 test; izole 5-dosya **canlı-hunk** rsync + elle gate `lint→965/965 test→build→restart→health 200`, adversaryal 3-QA 3/3, **migration YOK**; yedek `.deploy/cf-pool-fix-backup/`): CF havuzu `cf_customer_id` (=userId) başına **PAYLAŞILIR** ama `cf_remaining`/`cf_units_ordered` satır-bazlı tutuluyordu → **(BUG1 açlık)** en-erken-biten satır CF tavanındayken (`cf_units_ordered=daily_limit_snapshot`) gate hep onu seçip top-up yapamayınca headroom'lu kardeş havuzu dolduramıyordu = "hak var ama upstream_403" (bbbnull); **(BUG2 mirror ezme)** `updateCfRemaining` yalnız reserve satırına `Math.trunc`'la yazıp top-up bump'ından sonraki uçuştaki 403'ün taze-0'a ezmesiyle kilit (canlıda 51→1). **FIX:** Part A `updateCfRemaining(userId, remaining, source)` müşteri-bazlı TÜM kardeş satıra FLOOR'la yazar (`WHERE cf_customer_id=userId AND status='active' AND cf_units_ordered>0`); `source='success'`=CF otoriter header'ı KOŞULSUZ yaz, `source='error'`=403 body'si taze (<2sn) değeri AŞAĞI çekemez. Part B `topUpCfIfNeeded` müşteriyi entitlementId'den çözer, `poolBuffer=MAX(cf_remaining)` kardeşlerde düşükse en headroom'lu (`cap-ordered` DESC) kardeşten +50 alır (at-cap tetikleyen yerine), idempotency `topup-<candidate.id>-b<ordered>`. **Success-clobber güvenliği gate'in `cf_units_ordered < e.daily_limit_snapshot` headroom valfine BAĞLI** → o cümle KORUNMALI (`entitlement-service.test.ts` gate-headroom regresyonu kilitler). Doğrulama: 4 çok-satırlı müşteri (bbbnull dahil) her kardeş satırda AYNI `cf_remaining` (senkron), post-deploy 24/24 success. Bundan sonra **bbbnull/hobidiylab sınıfı elle pause/payg müdahalesi gereksiz** (gate at-cap satırı doğal baypas eder + kardeşten top-up). ⚠️ **Bu 3 kaynak dosya artık R-3'e EK olarak lokal `main`'den DAHA ileride** — yukarıdaki "canlıdan indir+hunk+rsync" kuralı `codefast-provisioning-service.ts`/`proxy.ts`'e de geçerli; `LOCAL_SRC=~/yzapi` deploy bu fix'i de geri alır; **manifest GÜNCELLENMEDİ**. Detay: `project_yzapi_cf_shared_pool_gate_fix` memory notu. Yardımcı scriptler: `scripts/cf-customer-usage.ts` (CF canlı havuz probe), `scripts/unlock-cf-entitlement.ts` (idempotent manuel top-up). **(6) ADMIN KULLANICI-PAKET YÖNETİMİ CANLI** (izole **4-dosya targeted rsync** + sunucu-içi gate `lint 0 / 972 test / build / scan temiz` → restart + health ok; subagent-driven 3-faz 2-aşamalı review + final review; **migration YOK**; yedek `.deploy/admin-pkg-mgmt-backup/`; `project_yzapi_admin_package_management` memory notu): admin panelinde kullanıcı detayında her pakete satır-içi aksiyon — **ekle** (her paket/her miktar, hediye veya bakiyeden tahsil), **değiştir** (günlük limit/kalan kota/bitiş/duraklat), **iptal** (+iade full/partial/none), **tek-başına iade**, **yenile** (gift/balance), **sil**. Hepsi owner-only, atomik (`dbSql.begin`+`FOR UPDATE`) + idempotent (UNIQUE key: `admin_grant_<idem>`/`admin_cancel_<entId>`/`admin_refund_<entId>_<gün>`) + audit. **Tüm yeni mantık izole 2 yeni dosyada:** `src/server/services/admin-entitlement-service.ts` (6 fn: adminGrant/Update/Cancel/Refund/Renew/Delete) + `src/yapayzekalab/tab-admin-user-packages.jsx` (`UserPackagesPanel`); `admin.ts`'e 6 route + `/users/:id/detail`'a `e.paused`, `tab-admin.jsx`'e tek import + "Paketler" bloğu değişimi. CF paketinde (`cf_units_ordered>0`) "kalan kota" elle düzenleme 400 ile REDDEDİLİR (ayna şişmesi yok). `entitlement-service.ts`/`proxy.ts`/`codefast-provisioning-service.ts`/`billing-service.ts` DOKUNULMADI. ⚠️ **KRİTİK LINEAGE BULGUSU (deploy sırasında kanıtlandı):** canlı `admin.ts`/`tab-admin.jsx` lokal `feat/official-prices` (`50b1019`)'in **DESCENDANT'I DEĞİL — farklı lineage**: canlıda `requireOwner` helper'ı + `Request,Response` import'u YOK (→ 6 route canlının kendi inline `if(req.adminRole!=="owner") return res.status(403)` desenine uyarlandı), canlıda zaten admin **hediye** özelliği var (`POST /users/:userId/gift-package` + 🎁 buton, `giftPackageToUser`), canlıda `live-state`/`codefast` admin sekmeleri YOK (o branch'te var). **Sonuç: `admin.ts`/`tab-admin.jsx`'e dokunan HER deploy canlıdan indirip hunk uygulamalı — `feat/official-prices` canlı değil; bu lokal branch'leri canlı sanma.** ⚠️ targeted rsync → **manifest GÜNCELLENMEDİ**.
- **Canlı durum (2026-06-22):** **Extended thinking 3-fix** (izole 5-dosya rsync, canlı kopyalara hunk uygulama — proxy.ts/closerouter-service.ts kontamine idi; 959/959 test, sunucu gate PASS). (1) `budget_tokens` eksik → upstream Pydantic 422: `buildRequestGuard()` artık `thinking.type==="enabled" && budget_tokens==null` ise otomatik enjekte. (2) `readProviderJson` HTML/non-JSON upstream yanıtında crash → 500: try/catch + anlamlı hata. (3) `anthropic-beta` header upstream'e iletilmiyordu: `handleTextJsonEndpoint` artık header'ı yakalar → `forwardMessages`'a `upstreamHeaders` olarak iletir. Yedek: `.deploy/thinking-fix-backup/`. Targeted rsync → manifest güncellenmedi.
- **Canlı durum (2026-06-21):** **CF top-up batch 150→50** izole 1-dosya deploy (`codefast-provisioning-service.ts`+test; 3/3 QA + çift onay + `rsync --checksum` izolasyon + sunucu gate 942/942; migration YOK; para yolu DOKUNULMADI; bkz `project_yzapi_cf_topup_batch_50` memory notu). Yeni `CF_TOPUP_BATCH_UNITS=50` → `topUpCfIfNeeded` batch'i artık 50 (ongoing lazy artış); `CF_INITIAL_BATCH_UNITS=150` **DEĞİŞMEDİ** (ilk order = soğuk-başlangıç 403 koruması, 2026-06-19'da bilerek 50→150 yapılmıştı — geri alma!); eşik `CF_TOPUP_THRESHOLD_UNITS=75` aynı. Amaç: kullanılmayan üniteye CF'ye peşin para verme (paket süresi bitince yanan ünite = sıfır maliyet; ilk 150 cold-start korur). ⚠️ targeted rsync → **manifest GÜNCELLENMEDİ**; yedek `.deploy/cf-topup-batch-50-backup/`. **HARD-WON CF gerçekleri (canlı reseller-API probe ile kanıtlandı):** (a) **STACK ÇALIŞIYOR** — aynı CF müşterisine (aynı `external_customer_id`) çoklu order = günlük havuz TOPLANIR (2×15 order → istek sonrası remaining 28.5) ⇒ **müşteri-çoğaltma/sharding GEREKSİZ**; gördüğümüz "150 tavanı" CF'nin per-customer cap'i DEĞİL, bizim istediğimiz order `limit_amount`'u (firstBatch); (b) **CF max süre 30 gün** (31 → 400 `ORDER_REJECTED "Requested duration is above reseller policy"`; kümülatif DEĞİL — 30g+1g ikisi geçer); (c) geçmişteki **265 top-up stall = süre bug'ı** (clamp öncesi top-up >30g süre yolluyordu → red → `cf_units_ordered` donar), `duration_days=min(30,…)` clamp 20 Haz'da düzeltti (20-21 Haz **0 hata**); (d) cost_units = **1.5/istek**, CF remaining KESİRLİ (remaining<1.5 → 403 `LIMIT_EXCEEDED`); (e) CF **idempotent** (aynı `external_order_id` tekrar = aynı order+debit, çift-charge YOK); (f) farklı `external_customer_id` → ayrı CF customer/key/havuz (dedup YOK) ama stack çalıştığı için gereksiz; (g) **"hayalet 201 kaldı" = top-up stall**, display bug DEĞİL → panel formülü (`kalan = limit − tüketilen`) top-up düzgün çalışınca DOĞRU/teslim-edilebilir olur ⇒ **panel=CF için ayrı kod gerekmedi** (drift'li `entitlement-service.ts`'e dokunulmadı). Paketleme açığı (500 satıyoruz / CF tek-order 150 verir → 500 stack ile) Ufuk "bekle" dedi.
- **Canlı durum (2026-06-20):** **2 izole deploy daha** (3/3 QA + çift onay + `rsync --checksum` izolasyon + sunucu gate; migration YOK; para yolu DOKUNULMADI; bkz `project_yzapi_cf_gate_counter_desync` memory notu): **(1) CF paket sayaç-ayrışması kök-fix** (`entitlement-service.ts`+`proxy.ts`) — CF paketi günlük `requests_today` cap'ine çakılıp panel `cf_remaining` "ünite var" gösterirken 402 veriyordu (402-with-units); gate CF paketini günlük sayaçtan muaf tuttu (yalnız cf_remaining kapılar) + `settleBilling` release `status!=='success'`; lokal itest kırmızı→yeşil + **canlı kontrollü test `fixProven:true`**. **(2) Panel kozmetik** (`entitlement-service.ts`+`admin.ts`) — yeni `computeDisplayConsumed` helper: bayat CF mirror'da `kullanilan:0`/dolu yerine gerçek başarılı-istek sayısı (3 yer: `listUserEntitlements`+`listUserPackagesForPanel`+admin; user-scope'lu `LEFT JOIN count(*) success`). Müşteri telafisi: kilitli entitlement'larda `requests_today = cf_units_ordered − cf_remaining` UPDATE (ödenmiş CF ünitelerini açar). ⚠️ İkisi de lokal git'te **UNCOMMITTED** (kontamine working tree → izole rsync, `admin.ts` admin-live-state kontaminasyonu ayıklandı); canlı yedekler `.deploy/cf-gate-fix-*` + `.deploy/cf-panel-display-*`.
- **Canlı durum (2026-06-19):** Bugün **3 izole deploy daha** (her biri 3-QA + çift onay + `rsync --checksum` izolasyon kanıtı + sunucu-içi gate; migration YOK; para yolu DOKUNULMADI; bkz `project_yzapi_cf_deadlock_renew` memory notu): **(1) CF lazy provisioning DEADLOCK kök-fix** (8 dosya) — gate artık CF-kalanı bazlı `(cf_units_ordered=0 OR cf_remaining IS NULL OR cf_remaining>0 OR cf_remaining_at < now()-'10 min')` **+ 10dk stale-0 self-heal supabı** (manuel rescue'yu kalıcı gereksiz kılar); `topUpCfIfNeeded` CF order başarılıysa AYNI UPDATE'te `cf_remaining = LEAST(COALESCE(cf_remaining,0)+batch, daily_limit_snapshot)` bump eder; `proxy.ts` success settleBilling call-site'ları artık `cfRemaining: usage.cfRemaining` taşır (mirror eskiden YALNIZ 403'ten yazılıp 0'a sıkışıyordu); top-up `duration_days = min(CF_MAX_DURATION_DAYS=30, ceil(kalan-ömür))` clamp'li (configurable `p.sure_gun`=90 şablon-tavanı CF policy'yi aşıyordu); EXTEND'de `cf_remaining=NULL` re-base + refund EXTEND'de revoke ETMEZ (over-revocation guard, `grantPackageEntitlement` artık `{entitlementId, extended}` döner). **(2) EXTEND-overwrite fix + "Paketimi Yenile"** (7 dosya) — `grantPackageEntitlement(txSql, params, ayriSatir=false)`: ayriSatir true → existing-lookup atla, HER ZAMAN yeni satır; purchase `ayriSatir = forceNewRow || pkg.is_configurable` → **configurable paketler artık EXTEND yerine AYRI SATIR** (eski EXTEND `daily_limit_snapshot`'ı EZİYORDU → ek paket alımı günlük limiti düşürüyordu); yeni `renewEntitlement` + `POST /api/user/entitlements/:id/renew` → `purchasePackageWithBalance(..., forceNewRow=true)` (Paketlerim'de "Paketimi Yenile" butonu, bakiyeden taze kota=yeni satır, **EK MONEY-PATH YOK**); `listUserPackagesForPanel.renewable`. ⚠️ **Çift-tahsil dersi:** renew idempotency key SUNUCUDA `renew_${entId}_${floor(Date.now()/90_000)}` (90sn pencere) türetilir — client per-tık random key'e GÜVENME (forceNewRow EXTEND-toplama emniyetini kaldırır). Çoklu aktif entitlement satırı `tryReservePackageSlot` tarafından zaten toplanır (`ORDER BY daily_limit ASC`). **(3) "Paketimi Yenile" butonu GEÇMİŞ paketlerde göster** (1 dosya `tab-mypackages.jsx`) — backend `listUserPackagesForPanel` `renewable`'ı **durumdan bağımsız** her `request_limit`+`!per_user_once` pakete verir (tükenmiş/süresi-dolmuş dahil) ama frontend butonu yalnız `PackageRow`'a koymuştu, o da sadece **aktif** bölüme map'leniyordu → "Geçmiş paketler" (Tükendi/Süresi-doldu) sade satır markup'ında buton render edilmiyordu (yenileme en çok tükeninçe istenir = buton tam gerektiği yerde yoktu). Fix: geçmiş satırlarına `{p.renewable && <button onClick={renew(p)}>}` eklendi (mevcut `renew()` + i18n keys + stil aynen; ek money-path YOK, aktif bölüm değişmedi; server-side 90sn idempotency = çift-tahsil yok). ⚠️ **UNTRACKED-frontend tuzağı (bu deploy'da kanıtlandı):** `tab-mypackages.jsx` (ve tüm Paketlerim/renew feature'ı) local git'te **UNTRACKED** — izole worktree'den deploy edilmiş, local main'e hiç commit'lenmemişti → `git diff` o dosya için BOŞ döner + `sync-deploy.sh` clean-tree guard ABORT eder. Çözüm: canlı dosyayı indir (`scp yzapi-vps:/opt/turkapiprojesi/<f> /tmp/`), local'le diff'le (yalnız kendi hunk'ın görünmeli = local==live+değişiklik), tek-dosya stage + `rsync --checksum` izolasyon kanıtı + live yedek + rsync + elle gate. Önceki taban (2026-06-18): VPS commit **`99e587c`** (health 200) — izole 5-dosya deploy (`ee7dd6d` üzerine): **panel tarayıcı geri/ileri tuşu fix** (tab-state SPA'da sekme değişimi ARTIK History API'ye yazılıyor `pushState` + yeni `popstate` dinleyici → Back/Forward SPA içinde sekmeler arası geziyor, OAuth redirect zincirine düşüp kullanıcıyı ATMIYOR; yeni `yapayzekalab/tab-routes.js` (`pathForTab`/`tabForPath`/`TAB_PATHS`) + `App.jsx` history-sync+popstate effect'leri + `App.tsx` initialTab; sadece frontend, billing/migration YOK; 3/3 QA; bkz `project_yzapi_panel_back_button_fix` memory notu). Bir önceki taban **`ee7dd6d`**: izole 4-dosya deploy (a42f54c üzerine): **x-api-key auth** (apiKeyAuth artık `x-api-key: yzk_live_` de kabul → Claude Desktop desteği; canlı 401-kanıtlı) + **Documents = hub + araç başına görselli rehber** (Codex/Claude masaüstü, Win/Mac OS-sekmeli, eski-anahtar `.bak` temizliği, kopyalanabilir gömülü key; bkz Frontend katalog + `project_yzapi_xapikey_docs_hub` memory notu). Migration EKLENMEDİ (0037'de kaldı). Bir ÖNCEKİ taban `a42f54c` (2026-06-18 seansı, 7 izole deploy): paket rebrand (CodeFast adı kaldırıldı → model-ailesi kategorileri) · **custom package builder** (configurable + hacim-kademeli marj, bkz Paketler) · custom-fiyat matrisi (codex 500/1000/2000 × 1/7/15/30) · redeem `copied_at` işareti + admin Kopyala · `per_user_once` · **free NVIDIA** (₺0 + per_user_once) · **iade-politikası zorunlu onay kapısı** (tab-account). Migration 0034→0037 uygulandı. ⚠️ Eski **2026-06-15 tabanı** (referans): VPS commit `e1503dc`. Progresyon: `e723152` (model-picker) → `7bcb2c6` (402 failover-eligible) → `59fafd1` (sampling-param strip) → `e1503dc` (IBAN iki-aşamalı WhatsApp bildirimi). Aktif özellikler: token-billing, i18n TR/EN, partner RBAC, görsel API, provider failover+circuit-breaker, **402 upstream failover-eligible**, **opus-4.7+/fable sampling-param strip** (bkz Billing Akışı), **3 GPT istek paketi (satista=false, "yakında satışta")**, **min yükleme $5**, paket upstream override (alanlar BOŞ), **Claude Code `/model` gateway-discovery uyumu** (bkz alttaki not), **IBAN iki-aşamalı admin WhatsApp bildirimi** (`/iban/init` "Ödeme hazırlanıyor" 1dk/kullanıcı notify-gate + 60sn dedup; yeni `POST /api/payments/iban/confirm` "Ödeme yapıldı" — müşteri wa.me + sistem otomatik, **DB-yazmaz/onay manuel**; `consumeActionRate` helper + `odeme_yapildi` notify kind; billing/migration DOKUNULMADI — bkz `project_yzapi_iban_two_stage_notify` memory notu). **Provider routing (2026-06-14 flip):** Claude (opus-4-8/4-7/4-6, sonnet-4-6, haiku-4-5) → **closerouter/popusk PRIMARY + wellflow fallback** (eskiden wellflow primary'ydi); GPT/Gemini/o-series → closerouter; rika=disabled. Maintenance notice özelliği lokal+QA tamam ama canlıda DEĞİL (Ufuk "beklet" dedi). ⚠️ **İzole-deploy tuzağı bu seansta da geçerliydi:** lokal `feat/codefast-reseller` branch live'ın ÖNÜNDE + working tree kirli (admin-live-state feature) → deploy `git worktree add --detach <dir> <live-commit>` + sadece ilgili commit'leri cherry-pick + `LOCAL_SRC=<dir> bash scripts/sync-deploy.sh` ile yapıldı; rsync `--checksum` dry-run ile izolasyon kanıtlandı (yalnız beklenen dosyalar görünmeli). ⚠️ **KONTAMİNASYON TUZAĞI (2026-06-18, canlıda kanıtlandı):** `admin.ts` ve `tab-admin.jsx` working tree'de ZATEN kirliydi (commit'siz admin-live-state). Bu dosyalara dokunan bir özelliği `git add <dosya>` yapınca commit, o kirli admin-live-state değişikliğini DE kaptı (`admin-live-state-service.js` import'u commit'e girdi ama dosyası commit'te yoktu) → izole-worktree deploy'unda **sunucu-içi `lint` (tsc) "Cannot find module" ile ABORT etti** (canlı GÜVENDE — restart olmadı). Ders: pre-kirli bir dosyaya dokunacaksan, izole worktree'de o dosyayı **canlı commit'ten al** (`git checkout <live> -- <dosya>`) sonra SADECE kendi değişikliğini hand-apply et; `git add <pre-dirty-dosya>` ile tüm working-tree halini commit'leme. Hangi dosyaların pre-kirli olduğunu `git status --porcelain` ile en başta gör. Yeni dosyalar (migration/service/test) güvenli — onlar pre-kirli değil.
- **Claude Code `/model` picker'ında Claude modellerini gösterme (gateway model discovery) — non-obvious, 2.1.177 binary'sinden çözüldü 2026-06-14:** Claude Code (native /v1/messages) `/model` listesini bizim katalogdan DOLDURMAZ; varsayılan yalnız `ANTHROPIC_MODEL`'e pinli modeli gösterir — discovery **opt-in**: müşteri `CLAUDE_CODE_ENABLE_GATEWAY_MODEL_DISCOVERY=1` set etmeli (≥2.1.129; docs Claude Code kurulum blokları artık bunu ekler). Flag açıkken Claude Code `GET {base}/v1/models`'i çeker ve **iki filtre** uygular: (1) id `^(claude|anthropic)`; (2) **dedup** — id Claude Code'un BUILT-IN model tablosuyla BİREBİR (string `===`, dot/dash normalize YOK) eşleşirse "From gateway" listesinden ELER. DASH-form `claude-opus-4-7/-4-6` + `claude-sonnet-4-6` built-in'de VAR → eleniyordu; yalnız `claude-opus-4.8` (NOKTA, built-in'de yok) sağ kalıyordu — "sadece 4.8 görünüyor" şikâyetinin kökü buydu. **Çözüm (`v1-catalog.ts` `clientFacingModelId`):** bu üç modeli `/v1/models`'te NOKTA-form id ile sun (`claude-opus-4.7` vb.) → built-in'e takılmaz → picker'da çıkar; etiket `display_name` alanından. NOKTA zaten geçerli alias (master-models aliases) → `/v1/messages` `canonicalizeModelId` DASH'e çevirir, **routing/billing/`model_overrides` AYNEN** (dash gönderen istemciler de çalışır). Remap YALNIZ `buildV1ModelsResponse` çıktısında; `filterV1ClientCatalogEntries` hâlâ master dash id'leriyle eşler.
- **Ops / devir / çalışma modeli yzapi DIŞINDA:** `/Users/ufuk/yeniapi/.kiro/` — `steering/agent-team-workflow.md` (BAĞLAYICI: lider + uygulama ajanı + **3 QA, ≥2 PASS**), `CALISMA-GUNLUGU.md` (append-only, en yeni ALTTA). **Devir tek-doğruluk:** `docs/AI_HANDOFF.md` + `docs/OPERATIONS.md` (canlı status ile doğrula — eski raporlar tarihsel).